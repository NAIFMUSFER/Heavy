import {substitutionReview,qty,$,$$,esc,money,number,parseMoney,date,badge,empty,get,getAll,post,patch,modal,closeModal,toast,busy,formData,identity,loginDialog,setupConnectivity,field,selectField,ticketCategories,ticketThread,downloadJSON,downloadText} from './common.js'; import {zoneMapMarkup,mountZoneMap} from './zone-map.js'; import {orderLinks} from './order.js'; import {locationFailure} from './address.js'; import {catalogImportTemplate,catalogImportCsvTemplate,catalogImportFromCsv,validateCatalogImport} from './catalog-import.js'; import {stockImportTemplate,stockImportCsvTemplate,stockImportFromCsv,validateStockImport} from './stock-import.js'; import {catalogReviewCsv,inventoryReviewCsv} from './ops-exports.js';
const root=$('#ops-app'),workspace=document.body.dataset.workspace;const state={user:null,page:'orders',orders:[],ordersNext:null,ordersGeneration:0,ordersLoading:null,procurementItems:[],procurementNext:null,procurementGeneration:0,procurementLoading:null,procurementFilter:'',healthGeneration:0,dashboardGeneration:0,launchGeneration:0,storefrontGeneration:0,storefrontDirty:false,reports:null,staff:[],catalog:null};
const staffRoles=[['admin','مدير'],['inventory','عمليات الموردين'],['picker','موظف الشراء'],['courier','التوصيل'],['support','الدعم'],['finance','المالية']];
const staffRoleName=role=>staffRoles.find(x=>x[0]===role)?.[1]||role;
const roleTitle={admin:'لوحة التشغيل',picker:'الشراء والجمع',courier:'التوصيل',finance:'المالية',support:'خدمة العملاء',inventory:'عمليات الموردين'};
function allowed(){if(!state.user)return false;if(workspace==='admin')return ['admin','finance','support','inventory'].includes(state.user.role);return state.user.role===workspace||state.user.role==='admin'}
function login(){root.innerHTML=`<div class="ops-login"><div class="panel stack"><div class="brand"><span class="brand-mark">ج</span><span><strong>جَنى</strong><small>بوابة التشغيل</small></span></div><h2>تسجيل دخول الموظفين</h2><p>الصلاحيات تُتحقق في الخادم. حساب العميل لا يفتح هذه الواجهة.</p><button class="btn primary" id="ops-login">تسجيل الدخول</button><a class="btn outline" href="/start.html">روابط المتجر والفريق</a><a class="btn outline" href="/">العودة للمتجر</a></div></div>`;$('#ops-login').onclick=()=>loginDialog(async u=>{state.user=u;if(!allowed()){toast('هذا الحساب لا يملك صلاحية هذه الواجهة',true);return}setInitialOpsPage();await render()})}
function menusForUser(){if(state.user.role==='inventory')return [['pickup-sites','الموردون ونقاط الاستلام'],['inventory','سجلات المخزون السابقة'],['disposals','الهدر والمرتجعات'],['supplier-credits','إشعارات الموردين'],['customer-returns','مرتجعات العملاء'],['movements','حركات المخزون']];if(workspace==='picker')return [['procurement','مهام الشراء'],['pickup-sites','دليل نقاط الاستلام'],['orders','التجهيز السابق']];if(workspace!=='admin')return [['orders','مهام التوصيل']];if(state.user.role==='admin')return [['dashboard','الرئيسية'],['launch','إعداد الإطلاق'],['orders','الطلبات'],['procurement','مهام الشراء'],['catalog','المنتجات'],['pickup-sites','الموردون ونقاط الاستلام'],['inventory','سجلات المخزون السابقة'],['disposals','الهدر والمرتجعات'],['supplier-credits','إشعارات الموردين'],['customer-returns','مرتجعات العملاء'],['movements','حركات المخزون'],['logistics','التوصيل'],['coupons','الكوبونات'],['finance','العهد والاستردادات'],['staff','الموظفون'],['customers','العملاء'],['audit','سجل التدقيق'],['support','الدعم'],['notification-jobs','الإشعارات الخارجية'],['health','سلامة النظام'],['storefront','إعدادات المتجر']];if(state.user.role==='finance')return [['dashboard','الرئيسية'],['orders','الطلبات المالية'],['procurement','مهام الشراء'],['finance','العهد والاستردادات'],['disposals','الهدر والمرتجعات'],['supplier-credits','إشعارات الموردين'],['customer-returns','مرتجعات العملاء'],['movements','حركات المخزون']];return [['orders','الطلبات'],['customer-returns','مرتجعات العملاء'],['support','الدعم'],['notification-jobs','الإشعارات الخارجية']]}
function opsMenusForUser(){return workspace==='courier'?[['procurement','عهد الاستلام'],['orders','مهام التوصيل']]:menusForUser()}
function opsPageFromHash(){
 const hash=globalThis.location?.hash||'',requested=/^#[a-z][a-z-]*$/.test(hash)?hash.slice(1):'';
 const menus=opsMenusForUser();return menus.some(([page])=>page===requested)?requested:menus[0][0];
}
function writeOpsHash(page,replace=false){
 if(globalThis.location&&globalThis.history&&globalThis.location.hash!=='#'+page)globalThis.history[replace?'replaceState':'pushState'](null,'','#'+page);
}
function setInitialOpsPage(){if(!allowed())return;state.page=opsPageFromHash();state.storefrontDirty=false;writeOpsHash(state.page,true)}
function discardOpsDraft(){return !state.storefrontDirty||confirm('لديك تعديلات على بيانات المتجر لم تُحفظ. هل تريد مغادرتها دون حفظ؟')}
async function navigateOpsPage(requested,fromHash=false){
 if(!allowed())return;
 const menus=opsMenusForUser(),page=menus.some(([key])=>key===requested)?requested:menus[0][0];
 if(page===state.page){writeOpsHash(page,true);if(!state.storefrontDirty&&!fromHash)await render();return}
 if(!discardOpsDraft()){writeOpsHash(state.page,true);return}
 state.storefrontDirty=false;state.page=page;writeOpsHash(page,fromHash);await render();
}
function shell(content){const menus=opsMenusForUser();if(!menus.some(x=>x[0]===state.page))state.page=menus[0][0];root.innerHTML=`<div class="ops-shell"><aside class="ops-sidebar"><a class="brand" href="/"><span class="brand-mark">ج</span><span><strong>جَنى</strong><small>التشغيل</small></span></a><div class="ops-user"><strong>${esc(state.user.name)}</strong><small>${esc(staffRoleName(state.user.role))}</small></div><nav class="ops-menu">${menus.map(([p,t])=>`<button data-page="${p}" class="${state.page===p?'active':''}">${t}</button>`).join('')}</nav><button class="btn outline" data-action="logout">خروج</button></aside><main class="ops-content"><header class="ops-topbar"><div><span class="eyebrow">${esc(roleTitle[state.user.role]||'التشغيل')}</span><h1>${esc(menus.find(x=>x[0]===state.page)?.[1]||'جَنى')}</h1></div><button class="btn outline" data-action="refresh">تحديث</button></header>${content}</main></div>`}
const snapshotAddress=o=>[o.snapshot?.address?.city,o.snapshot?.address?.district,o.snapshot?.address?.street,o.snapshot?.address?.details].filter(Boolean).join('، ')||'العنوان محفوظ في الطلب';
function deliveryLinks(o){const a=o.snapshot?.address||{},links=orderLinks(a);return `<div class="ops-actions">${links.directions?`<a class="btn outline" data-delivery-directions href="${esc(links.directions)}" target="_blank" rel="noopener noreferrer">بدء الملاحة للتوصيل</a>`:''}${links.phone?`<a class="btn outline" data-delivery-phone href="${esc(links.phone)}">الاتصال بالمستلم</a>`:''}</div>`}

function task(o){const picker=workspace==='picker'||(workspace==='admin'&&state.user.role==='admin');const courier=workspace==='courier';const finance=workspace==='admin'&&['admin','finance'].includes(state.user.role);let actions='';if(workspace==='admin'&&state.user.role==='admin'&&o.status==='active'&&(['queued','picking','awaiting_customer'].includes(o.fulfillment_state)||(o.fulfillment_state==='ready'&&['unassigned','assigned','failed'].includes(o.delivery_state))))actions+=`<button class="btn outline" data-action="assign-order" data-id="${esc(o.id)}">إسناد الموظف</button>`;if(picker&&o.fulfillment_state==='queued')actions+=`<button class="btn primary" data-action="start" data-id="${o.id}">بدء التجهيز</button>`;if(picker&&['picking','awaiting_customer'].includes(o.fulfillment_state))actions+=`<button class="btn primary" data-action="open-pick" data-id="${o.id}">الأوزان والبدائل والتجهيز</button>`;if(courier&&o.fulfillment_state==='ready'&&['unassigned','assigned','failed'].includes(o.delivery_state))actions+=`<button class="btn primary" data-action="dispatch" data-id="${o.id}">استلام المهمة وبدء التوصيل</button>`;if(courier&&o.delivery_state==='out_for_delivery')actions+=`<button class="btn outline" data-action="share-location" data-id="${o.id}">مشاركة موقعي الآن مع العميل</button><button class="btn primary" data-action="deliver" data-id="${o.id}">تأكيد التسليم</button><button class="btn danger" data-action="fail" data-id="${o.id}">تعذر التسليم</button>`;if(courier&&o.delivery_state==='delivered'&&o.payment_state==='awaiting_collection')actions+=`<button class="btn primary" data-action="collect" data-id="${o.id}" data-total="${o.total_halalas}">تسجيل تحصيل ${money(o.total_halalas)}</button>`;if(finance&&o.cash_state==='with_courier'&&Number.isSafeInteger(o.cash_liability_halalas))actions+=`<button class="btn primary" data-action="settle" data-id="${o.id}">تسوية عهدة ${money(o.cash_liability_halalas)}</button>`;if(finance&&o.collected_halalas>o.refunded_halalas)actions+=`<button class="btn outline" data-action="paid-refund" data-id="${o.id}">تسجيل مبلغ أُعيد للعميل</button>`;return `<article class="task-card"><div class="row between"><h3>${esc(o.number)}</h3>${badge(o.status)}</div><p>${esc(snapshotAddress(o))}</p>${courier?deliveryLinks(o):''}<div class="row wrap">${badge(o.fulfillment_state)} ${badge(o.delivery_state)} ${badge(o.payment_state)} ${o.cash_state?badge(o.cash_state):''}</div><div class="task-meta"><span>${date(o.created_at)}</span><strong>${money(o.total_halalas)}</strong></div><div class="ops-actions">${actions}<button class="btn outline" data-action="detail" data-id="${o.id}">التفاصيل</button></div></article>`}
function clearOrderPages(){state.ordersGeneration++;state.ordersLoading=null;state.orders=[];state.ordersNext=null}
async function ordersPage(more=false){
 if(!state.user||state.page!=='orders'||(more&&(!state.ordersNext||state.ordersLoading!==null)))return;
 const user=state.user,generation=++state.ordersGeneration,query=new URLSearchParams({limit:'50'});
 if(more)for(const [key,value] of Object.entries(state.ordersNext))query.set(key,String(value));
 state.ordersLoading=generation;
 try{
  const r=await get('/api/ops/orders?'+query);
  if(state.user!==user||state.page!=='orders'||state.ordersGeneration!==generation)return;
  state.orders=Array.from(new Map([...(more?state.orders:[]),...(r.items||[])].map(o=>[o.id,o])).values());
  state.ordersNext=r.next||null;
  shell(`<section class="stack"><p role="status">المعروض: ${number(state.orders.length)} طلبًا${state.ordersNext?' · توجد طلبات أقدم':''}</p><div class="task-grid">${state.orders.map(task).join('')||empty('لا توجد مهام','لا توجد طلبات تحتاج إجراء الآن.')}</div>${state.ordersNext?'<button class="btn outline" data-action="orders-more">عرض طلبات أقدم</button>':''}</section>`);
 }catch(error){
  if(state.user!==user||state.page!=='orders'||state.ordersGeneration!==generation)return;
  if(error.code==='AUTH_REQUIRED'){state.user=null;clearOrderPages();login()}
  throw error;
 }finally{if(state.ordersLoading===generation)state.ordersLoading=null}
}

async function shareCourierLocation(order){
 if(!navigator.geolocation)throw Error('تحديد الموقع غير متاح في هذا المتصفح.');
 const point=await new Promise((resolve,reject)=>navigator.geolocation.getCurrentPosition(resolve,e=>reject(Error(locationFailure(e))),{enableHighAccuracy:true,timeout:12000,maximumAge:0}));
 const result=await post('/api/ops/orders/'+order.id+'/location',{latitude:point.coords.latitude,longitude:point.coords.longitude,accuracy_m:point.coords.accuracy});
 toast('تمت مشاركة موقعك لهذا الطلب · '+date(result.created_at));
}
