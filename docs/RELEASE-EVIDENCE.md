# Release evidence — 10 September 2026

This is an engineering checkpoint, not a commercial launch certification.

| Evidence | Verified result |
|---|---|
| Render gateway commit 92850d3 | Live; complete verification and smoke succeeded in Actions run 34397238501 |
| Render gateway commit 44760b5 | Deployment dep-dagrn6mq1p3s738l6ajg is live; complete verification and smoke passed, run 34398693638 |
| Android revision 4cdf32f | Native APK build and artifact upload succeeded, run 34395725064 |
| iOS revision 4cdf32f | Simulator app compile succeeded, run 34395725150 |
| Address/mobile revision ae46c74 | Database, Expo export, Android APK (34400191895), and iOS simulator (34400191843) all passed |
| Empty database replay | Recovered schema and all current migrations pass in disposable PostgreSQL 17/PostGIS; run 34400862608 |
| Database tests | 34 transactional regression checks, ten sixteen-client concurrency groups, ten coupon lifecycle/pricing groups, eight address groups and four scheduled-worker checks passed |
| JavaScript tests | 137 passed locally, zero failed |
| Supabase Edge | jana-api v18, jana-critical v5, jana-ops-extra v13 deployed ACTIVE |
| Production invariants | Negative stock, excess reservation, slot overbooking, duplicate quote orders and cash invariant counts all zero |
| Security grants | Zero anon/authenticated EXECUTE grants on new coupon writes/triggers |
| Coupon release | Database/API/web/mobile implementation present; server flag disabled without dedicated JANA PostHog configuration |

Failures were corrected and rerun: Docker health-command quoting; a health predicate incorrectly flagging refunded settlements; ambiguous SQL in quote-expiry resource release. Earlier failed/cancelled runs remain part of the audit history.

The recovered original migration files retain their exact contents. The recent repair filenames were aligned with Supabase's actual applied timestamps after deployment; SQL contents were not changed.

Remaining launch blockers include real commercial catalog/receipts/coverage and business policies, dedicated analytics configuration, PostGIS owner-grant findings, comprehensive substitutions/refunds/finance journeys, warehouse counts/cost completeness, provider adapters, backup data restore and browser/device acceptance. The master Definition of Done is not yet met.

Database scheduler `jana-quote-expiry` is installed and active every minute. Production execution succeeded on 9 September at 20:28 UTC and again on 10 September at 02:11 UTC. Render commit `42b46ce59b1ac5c6bd5dcee9175452bab8598008` is live (deployment `dep-dags8om7bikc73bram5g`).

Inventory cost migration `20260910021130` is applied. Feature commit `ffb3411` passed database run **34402318961**, including ten cost-ledger groups and fifteen Edge RPC schema contracts, and verification run **34402318960**. The append-only ledger records physical movements and proportional consumed purchase cost, preserving unknown cost and marking found-stock estimates. Seven-day reports suppress profit/margin when source cost is incomplete. All eight existing preview lots have recorded source costs; there are no delivered production orders or historical consumption entries to backfill. Application RLS now covers 34 tables; new ledger direct-client grants are zero. Production stock, reservation, capacity, duplicate-order and cash invariant checks remain zero. Reporting UI is live at `9257a412` (Render `dep-dah16j95efls73fi4cig`); live-smoke run **34428824564** and database run **34428824486** succeeded.

Assembled operations-page tests caught an older dashboard override that would replace the new report and coerce unknown margin/profit to zero. The duplicate override was removed; unknown, zero and negative financial values now pass two rendered-page tests. Total local JavaScript tests: 91 passed. The additional first negative-margin assertion was corrected to account for the Arabic locale direction mark.

Product model migration `20260910022807` is applied after database run **34429291865** passed thirteen product groups, seventeen RPC schema contracts and the earlier transaction suites. Canonical families and product versions now contain multiple sellable sizes; stable sellable lineages preserve favorites/lists compatibility. Draft creation and confirmed activation are separate. New quotes preserve canonical family/version/size identifiers. Eight existing offerings were mapped without changing their rows; before/after offering and original-order-snapshot hashes are identical. RLS covers 37 application tables. Product administration is live at `95ea6efc` (Render `dep-dah1fhh42hec73ec2qd0`); live smoke **34429865092** and database **34429865043** passed. Manual authenticated admin/browser acceptance remains unverified.

Support migration `20260910024105` applied after database run **34430171074** passed fifteen support lifecycle groups. Customer and staff replies now use the same normalized message contract; waiting-for-customer state, validated assignment, priority, close/reopen, concurrent replies and persisted idempotency are tested. Conversation edits/deletion are rejected, state changes audited, and notifications are in-app only. There were no existing production tickets to rewrite. New support RPC client grants are zero; prior PostGIS owner findings remain unchanged. Support UI is live at `308304c8` (Render `dep-dah1o05bedkc738nooj0`); smoke **34430997857** passed. Support Android **34430509624** and iOS simulator **34430509588** both passed.

Cash/refund migration `20260910030745` applied after **34431933621** passed all seventeen financial groups and earlier suites. Ledger reconciliation, payment-source separation, partial settlements, duplicate references, refund requests and completion/rejection are enforced. Original order snapshots are unchanged, cash-ledger mismatch count is zero, and there are no direct client grants on new financial RPCs. RLS covers 38 app tables; PostGIS owner findings remain unresolved. Financial UI release is pending. The service-worker cache-first defect was repaired with network refresh and offline fallback; four tests cover fresh assets, offline behavior, failed responses and cache quota failures. Gateway analytics now requires dedicated JANA project configuration; generic keys cannot enable capture.


Substitution migration `20260910033534` is applied. Database **34433806064** passed sixteen substitution groups, twenty-nine Edge/PostgreSQL signature contracts, all earlier transaction suites and the scheduled-worker test. Initial run **34433511081** failed only because a test parsed PostgreSQL boolean text as JSON; corrected JSON serialization passed the assertion. The original production order row fingerprint `d6077d5d61b0f28fd6fd2388cd4d292f` is identical before/after application. Client EXECUTE grants on JANA RPCs remain zero, reallocation helper execution by service_role is false, and new issue-table RLS is enabled. API v16 deployed. Existing PostGIS owner findings persist. Web/mobile release pending exact public-commit verification.

Financial release `0176a0b4` compiled Android **34432911178**, iOS simulator **34432911168** and Expo **34432911173** successfully; database **34432911242** passed. Render deployment `dep-dah23obl550s73d8i5bg` reported live at 03:20:46 UTC. Smoke **34432911313** attempt 1 failed on a 502 during rollout; attempt 2 failed because public `/version` still returned `308304c8` throughout the readiness window. Independent public requests confirmed old commit plus healthy dependencies at 03:34 UTC. This release is NOT VERIFIED on the public route. The mismatch is retained as evidence and must be resolved by a subsequent verified deployment.


Corrective combined release **fcb7f8f1f41b15066249beba4a300743b5418ebb** is verified on the public URL. Render `dep-dah2dddbedkc738qef00` completed at 03:41:11 UTC after a build with cleared cache. Live smoke **34434232333** passed nineteen checks, and database **34434232313** passed. Independent requests confirmed exact `/version`, all three ready dependencies and service-worker v4. This supersedes the unverified 0176 deployment; the stale-routing root cause was not established. Substitution Android **34433806118**, iOS simulator **34433806132** and Expo **34433806149** all passed on identical mobile source. Financial and picking/customer-substitution interfaces are now deployed; authenticated browser/device and real business acceptance remain unverified.

Inventory count migration `20260910035511` applied after **34434887966** passed fifteen warehouse groups, thirty-six RPC argument contracts and all prior suites. Before/after production inventory fingerprint `34ca43d93d066dd58dcfa56c312f96f4` matches; existing quantities, reservations and costs did not change. Zero client EXECUTE grants remain on JANA RPCs; count-session RLS enabled. Existing PostGIS owner findings persist. Operations Edge v13 deployed; warehouse UI release pending.

Warehouse release **8f5ff5ca99c5afa5bc7202c90423e180c69aca53** is verified live (Render `dep-dah2osp42hec73eherdg`, 04:05:42 UTC). Exact public `/version` and `/ready` succeeded. Live smoke **34435760965** passed; database **34435760925** passed.

Staff backend **e5270fb2** passed database **34436087456**, including fourteen staff groups and forty-one RPC argument contracts. Node verification **34436087463** passed. A subsequent review found the customer anonymization RPC also needed to reject staff; that guard and its regression test are pending the next database gate. Local UI/API suite: **137 passed**, zero failed. The first staff-dialog test failed because its DOM stub did not model a connected button; corrected stub passed. Production preflight found zero active administrators; no account was changed. Staff migration and UI are not yet deployed.

Staff migration **20260910041948** applied after **34436636396** passed fifteen staff groups, forty-one RPC contracts and all preceding suites. Verification **34436636401** passed. Staff membership fingerprint `74e152ae242857f6f47c4d978c236fb0` and order assignment/total fingerprint `ae57746dac0dcfb464b76bbe127066dd` match before/after. JANA RPC client execute grants remain zero; the private transition helper is not service-executable. All production business invariants report zero violations. Existing PostGIS owner findings remain unresolved. Main API v17 deployed; exact web release verification pending.

Staff web release **e67c0f476c4b9ac6119c088a2265a5b1f747dd00** is live (Render `dep-dah32r3l550s73dcgo90`, 04:26:47 UTC). Exact-commit live smoke **34437124783** and database **34437124787** succeeded. Owner initialization remains required; no customer or staff account was created during verification.

Saved-list/recurring implementation remains unpublished to production. Database **34437458476** failed at initial list creation; qualified SQL variable names resolved it. Run **34437706014** then passed eleven groups, including list concurrency and calendar month anchors, and identified incorrect SQL string concatenation in the reminder body. That expression is corrected and the complete worker/scheduler gate is being rerun. Neither failed build is a PASS.

Customer saved-data migration **20260910044830** is applied. **34438196238** passed fifteen saved-list/profile/reminder groups, including actual pg_cron execution, fifty RPC argument contracts and all earlier suites. Android **34438196280**, iOS simulator **34438196304**, Expo **34438196312** and Node **34438196251** succeeded on source **32800e29**. Initial management-tool apply returned an internal transport error; read-only probes confirmed no migration/columns/job existed before the successful retry. There remain zero production lists/plans, all business invariants are healthy and JANA client RPC grants are zero. Recurring job is active every minute. API v18 deployed. Existing PostGIS owner findings remain unresolved. Exact web release verification pending.

## 10 September — saved customer release and first browser gate

Saved lists/reminders/profile release `36572f34574cdc03fc65fa5338346ae149e92967` is verified live. Render `dep-dah6ich42hec73f10v7g` finished at 08:24:45 UTC; exact-commit smoke `34454949237` passed. Previous feature DB `34438741655` and saved UI native Android `34438196280`, iOS `34438196304`, Expo `34438196312` passed. Production reminder worker executed successfully after migration 20260910044830; no production reminder plan or order was invented.

First isolated browser gate `34455369503` at `d533ed4166dd2d1e2fecc991d9a8f5e310e00c65` **failed** after successful schema replay, fixture setup, local PostgREST and Chromium installation. The actual inventory user logged in, but the supplier form's POST returned 403. Its existing RPC allowed admin only despite the inventory interface and required supplier workflow. The forward repair permits admin/inventory and routes creation through persisted warehouse idempotency; customer/courier rejection and eight concurrent retries are tested. This repair is not yet applied or verified at this checkpoint. Fixture screenshots/logs are retained in the failed run. Production permission defaults were not weakened to get the test past its failure.

Second browser gate `34455786633` at `66d11a5` passed warehouse receipt/inspection, secure customer cookie registration/restoration, quote stock/slot reservation, order confirmation, assigned actual-weight picking and OTP delivery. It then failed waiting for the courier collection button: the old `jana_ops_orders` query filtered `status=active`, hiding delivered orders even though cash remained uncollected. The new forward migration preserves assigned delivered tasks until collection/liability is resolved. The test harness also now observes action and response promises together so timeout failures retain their diagnostic artifacts. Database gate `34455786610` passed the supplier repair and all prior suites. Overall browser acceptance is still pending.

Third browser gate `34456089554` at `10e6b98` passed through separate courier collection and full finance settlement, then failed at the support reply form. The shared select helper marked every select required, so its explicitly optional unassigned-agent value prevented native form submission. The helper now accepts an explicit required option; support assignment and receipt supplier are optional, while payment source remains required. Database `34456089531` and Expo `34456089562` passed; complete browser result remains pending.

Browser gate `34456468946` at `3d0d74504cc0b0ffe6bee0620dc0e2b768994045` **passed all 14 checks** at 08:41 UTC with actual PostgreSQL/PostgREST, gateway/Edge handlers and seven browser role interfaces. The 149 local JavaScript tests pass (including three mobile pagination and nine provider boundary tests). New provider classes are not enabled external services; the missing outbox/worker and concrete future adapters are documented.

Migrations applied at 08:41 UTC: `20260910084133_jana_warehouse_supplier_creation.sql`, `20260910084148_jana_courier_cash_tasks.sql`. SQL is unchanged from successful DB run `34456089531`; only draft filenames are aligned with actual migration history. Operations Edge v14 is active. Order money fingerprint `2cfeaac02cc9871bdae22b71fff0d041` and stock fingerprint `3cebfb6900970f340d1bd04c656a19d9` are unchanged. Critical production invariants all report zero violations, and the three modified RPCs have zero direct anon/authenticated EXECUTE grants. Advisor findings remain limited to the previously documented Supabase-owned PostGIS objects. No administrator was activated and no production test transaction was created.

Release `ba912e00638508bed6a58aaba9cccca587e1114f` became live in Render `dep-dah6snh5efls7389jnv0` at 08:46:50 UTC. Exact-commit smoke `34456959244`, browser `34456959110` and DB `34456959116` passed. Mobile pagination source `10e6b98` passed native Android `34456089553` and iOS `34456089548`; Expo `34456089562` passed.

Delivery administration source `d1b6661` passed the existing browser journey (`34457399575`), but its new DB suite `34457399618` failed in slot-creation concurrency setup after five groups passed. The fixture reused an existing `(zone_id, starts_at)` unique combination; the corrected fixture creates a distinct start time and retains per-attempt errors. The unique constraint was preserved. Expanded delivery forms and browser coverage are pending validation; the draft delivery-administration migration is not applied.

Delivery DB run `34457720540` passed eleven new administration groups and all prior suites. Extended browser run `34457720531` failed when opening the edit-zone form: the new edit actions were missing from the click dispatcher. Review also found that the old catalog summary omitted polygon, metadata and revision required by the editor. Both bindings are corrected, with a database assertion for the editable zone payload. The delivery draft remains unapplied pending the corrected browser/DB gates.

Delivery browser `34457996202` reached the real edit form and found a gateway routing gap: PATCH zone/slot item paths were absent from the fixed operations-route allowlist. Added only those canonical item paths, with identifier redaction and a gateway regression. No arbitrary proxy destination is introduced. The delivery release remains pending until its extended journey passes.

Delivery browser `34458234675` at `c253f77bb06651a4cbf90965cfbd0287f3eee63e` **passed all 15 checks**, including real zone and slot creation/editing, at 09:01 UTC. Node `34458234713` passed; database `34457996178` passed the identical SQL and Edge source, including eleven delivery groups and all preceding suites. The local JavaScript suite has 150 passes and zero failures. Native Android `34456959099`, iOS `34456959171` and Expo `34456959218` also passed the existing production source.

Delivery migration `20260910090725` was applied at 09:07 UTC without changing its tested SQL. Operations Edge v15 is ACTIVE. Full order fingerprint `d6077d5d61b0f28fd6fd2388cd4d292f` and stock-balance fingerprint `191d727994f8e1876d9c6e3db394a91c` match before and after. Stock, reservation, slot, duplicate quote-order and cash violation counts are zero. All JANA RPC client execute grants remain zero; the private zone and slot helpers cannot be executed by service_role. The security advisor still reports the previously documented Supabase-owned PostGIS findings; no system extension objects were altered. There remain zero active production administrators. Gateway promotion is pending at this checkpoint.

Delivery release `25d4d60d75b9b26ed1ce6ffceb23c8aac857638e` is verified live in Render `dep-dah794mq1p3s73auet7g`, finished 09:13:30 UTC. Exact-commit live smoke `34459328788`, database `34459328891` and browser `34459328824` succeeded. The renamed migration replay also passed `34459059935` before promotion.

Catalog source `840012da8afcd973e38577de89d7c61614ce7201` passed database `34459538395`, browser `34459538351` and Node `34459538368`. Its eight catalog groups verify 123 same-timestamp offerings, payload compatibility, literal filters, unavailable lots, bounds and grants. Local JavaScript suite: 152 passed. Applying the tested catalog migration and API is the next deployment step.

Catalog migration `20260910091656` is applied and main API v19 is active. Actual first-page database reads return two items and next offset 2. Order fingerprint `d6077d5d61b0f28fd6fd2388cd4d292f` and stock fingerprint `191d727994f8e1876d9c6e3db394a91c` are unchanged. New RPC client grants are zero, and the previously recorded PostGIS advisor findings persist. The saved-cart feature and expanded browser journey are still pending verification and are not yet deployed.

Saved-cart source `1d7063f` passed Node `34460270942` and Expo `34460270898`, but browser `34460270896` failed because the gateway method allowlist rejected PUT. The corrected gateway permits PUT only for `/api/cart`. Database `34460271012` passed seven cart groups and then failed because the version-change fixture used an unmapped legacy offering; the fixture now creates and activates a canonical product family before exercising version resolution. Neither failed run is a PASS. Review also aligned cart selection limits with the existing 40-line checkout boundary and corrected the cart subtotal update after quantity changes. The saved-cart migration remains unapplied.

Saved-cart repair `2d6e08c` passed database `34460588099`, including eleven cart groups and all previous suites, Node `34460588122` and Expo `34460588160`. Browser and native outcomes remain pending at this checkpoint. The new customer basket composer is being added to the actual browser journey before checkout; its web/mobile source remains under validation.

Browser `34460588056` passed all sixteen checks at source `2d6e08c`, including saved-cart restoration through a second authenticated browser. Its native runs were cancelled by a newer source revision, not passed. Cart migration `20260910092949` is applied and API v20 is active. The table has no production cart fixtures. Original order and stock fingerprints are unchanged; cart RLS is enabled, 41 application tables have RLS, and all JANA RPC client grants remain zero. Existing Supabase-owned PostGIS findings are still unresolved.

Custom composer source `fc212c0aaa12515d0887588f2d9a6a037fbf48a4` passed the expanded **17-check browser journey** in `34460851557`, Node `34460851477` and Expo `34460851584`. Database source is unchanged from successful `34460588099`. Native Android `34460851476` and iOS `34460851533` are in progress. Public web promotion is pending; this checkpoint does not call the new source live.

Composer and saved-cart mobile source `fc212c0` passed Android APK `34460851476` and iOS simulator `34460851533`. Release `fe9fcb557d5b0e839b8c2b6a4a0095d521692ca6` became live in Render `dep-dah7kjpt0dsc73etsr4g` at 09:37:47 UTC. Exact-commit smoke `34461586342`, browser `34461586321` and DB `34461586347` passed. Saved cart and custom composition web interfaces are now deployed; actual mobile device acceptance and signed distribution remain unverified.

Optional-email/phone-password account changes are under validation. Preflight found zero existing nonstandard phone rows or normalized phone collisions. No production account or contact was changed. The candidate migration does not enable SMS verification or recovery. Local JavaScript suite is 160 passes with zero failures; real database/browser/native gates remain required.

Optional-email source `d89fb4a8d176dc36676f2874d666e2474d3c0e3d` passed database `34462074508`, browser `34462074459`, Node `34462074591`, Expo `34462074506` and iOS simulator `34462074546`. Android remained in progress at this checkpoint. Migration `20260910094542` is applied and API v21 is active. User fingerprint `081d2d6e2c7088c3473cfbbc75cfa93d`, order fingerprint `d6077d5d61b0f28fd6fd2388cd4d292f` and stock fingerprint `191d727994f8e1876d9c6e3db394a91c` are unchanged. Client RPC grants remain zero and no administrator was activated. The existing PostGIS owner findings persist. Customer-directory source is under validation and has not been applied.

Customer directory source `28032cc` passed database `34462706968`, including eight directory/privacy groups, and Node `34462706925`. Browser `34462707022` failed earlier in warehouse receipt: after supplier creation invalidated the catalog, opening the receipt before the background refresh completed produced an empty stock selector. Receipt and delivery-settings dialogs now explicitly await their required fresh catalog; click handlers await and report errors. A delayed-data UI test verifies the receipt does not render an empty fallback. The failure is retained rather than attributed to a browser flake. The customer-directory migration is not yet applied.

Customer-directory repair aa0c00e passed Node 34463088503 and browser 34463088510, including all **19 journey checks**. The identical SQL/Edge source passed database 34462706968 and its eight directory/privacy groups. Local JavaScript suite: **162 passed**, zero failed. Phone-account Android 34462074478 also succeeded, alongside the previously recorded iOS/Expo passes. Migration **20260910095523** is confirmed applied and main API **v22** is active. The migration was verified read-only after a session interruption and was not applied twice. User, order and stock fingerprints remain unchanged, application client RPC grants remain zero, and no administrator was activated. Existing Supabase-owned PostGIS advisor findings remain unresolved. Gateway promotion is pending at this checkpoint.

Phone-account and customer-directory release **388b777d31b8f3676f57b949968f328f0d3c25ff** is verified live. Render **dep-dah81d942hec73f6o0ug** completed at 10:05:06 UTC; exact-commit live smoke **34464111345**, browser **34464111371** and database **34464111376** succeeded. The renamed migration replay also passed feature database 34463956177. No production fixture account or order was created during verification.

Weight-policy source c68ec4b passed local 163 JavaScript tests, Node 34464859799 and Expo 34464859824. Browser 34464859847 completed the earlier journey and real product-version creation, then failed because the test tried to click activation inside a collapsed product-details panel. The test now opens the actual review panel before activation. No application permission or visibility rule was bypassed. Database/native gates and corrected browser acceptance are still pending; the weight migration is unapplied.

Weight policy database **34464859924** passed all fourteen new groups and preceding transaction/concurrency suites. Corrected browser **34465137608** at 45fdb3a passed all **21 journey checks**, including real admin policy creation, customer quote review and extra-weight FEFO consumption at the unchanged price. iOS simulator **34464859821** and Expo **34464859824** passed on c68ec4b mobile source; Android remained in progress at this checkpoint. Migration **20260910101808** is applied; API **v23** and operations Edge **v16** are active. Prior offering fields fingerprint 02136a56b00b3558d9e919af1be2014e, original orders fingerprint d6077d5d61b0f28fd6fd2388cd4d292f and stock fingerprint 191d727994f8e1876d9c6e3db394a91c are unchanged. Zero existing offerings opted into overfill. All production business invariants and JANA direct-client RPC grant counts remain zero; both new private helpers are not service-executable. Existing PostGIS owner findings persist. Gateway promotion is pending.

Weight-policy mobile source c68ec4b passed Android **34464859783** and iOS **34464859821**. Android debug artifact 10147302902 is available until 17 September; the existing iOS gate verified `.app` generation but did not retain a downloadable artifact. Weight web release **a7cd2bf3b2d6fa7d4628d076f8ee2c914813224d** is verified live in Render **dep-dah8a6ajnfac738o6jag** at 10:23:45 UTC. Exact-commit smoke **34465769607**, browser **34465769597** and database **34465769675** passed.

Release review found mobile dependency installation lacked a committed lock and used `npm install`; iOS had no retained simulator artifact. A read-only CI job now generates a public-registry lock candidate for checksum review. Native/Expo pipelines are being converted to `npm ci` and Node 24; iOS archive upload is being added. These changes are pending validation and do not retroactively make older builds reproducible. Local offline lock generation failed because the package registry metadata was not cached; no credentials or network permission were fabricated.

Lock candidate run 34465948015 succeeded. The complete candidate was recovered from its read-only CI output and verified against SHA-256 `9afea8f706d4d27f1f755d3c705c0b81a23ea440c6c5171fcc8fce1c42e0e410` before writing `mobile/package-lock.json`. It resolves 748 package records (Expo 54.0.37, React Native 0.81.4) and exactly matches the existing direct dependency manifest; every resolved package URL is the public npm registry without embedded credentials. Locked Node 24/native/Expo rebuilds are required next.

Pinned dependency source **716bb7caf8edd0a5de4a6398f41472ca39dca034** passed Node **34466248755**, Expo **34466248726**, PostgreSQL **34466248816**, browser **34466248737**, Android **34466248708** and iOS **34466248711**. Both native artifacts were retained: Android **10147846242** and iOS simulator **10147775727**, expiring 17 September. These builds use Node 24 and `npm ci` from the checked-in lock. API **v24** is active; it removes a duplicated error mapping while preserving the prior INVALID_WEIGHT error code. Exact gateway release verification is pending at this checkpoint.

Pinned release **716bb7caf8edd0a5de4a6398f41472ca39dca034** is verified live. Render **dep-dah8gf2jnfac738ovnj0** finished at **10:37:16 UTC**; exact-commit smoke **34466920786** and browser **34466920733** passed. Identical-head feature database 34466248816 and both native builds already passed; duplicate production-branch native/database jobs were still running when this checkpoint was recorded and are not reported as completed. All native artifact links and unresolved owner/engineering requirements are in DELIVERY-REPORT.md.

Owner initialization completed **2026-09-10 14:19:39 UTC**, after the owner supplied their signed-in account screenshot to resolve the previously mistyped email. The exact registered account was an active customer with no customer orders, live quote or operational obligations. A guarded transaction under the existing staff-membership advisory lock changed only that account's role to admin, revoked one session and wrote one `owner_initialized` audit event with before/after state and operation identifier. A subsequent read independently verified one active administrator, the audit record, unchanged order fingerprint `d6077d5d61b0f28fd6fd2388cd4d292f`, unchanged stock fingerprint `191d727994f8e1876d9c6e3db394a91c`, and zero anon/authenticated EXECUTE grants on JANA functions. No password was read or reset, no legacy account reactivated, and no schema or runtime source changed. The owner must sign in again; authenticated owner operation remains NOT VERIFIED. This data operation did not rerun or add to the automated test counts above.

Visual zone editor implementation passes the local **170-test** JavaScript suite and assembled bundle syntax checks. New browser assertions draw real geographic exterior/interior rings, compare saved database coordinates, and exercise coordinate editing/undo during a commercial revision. That browser gate has not yet run. The editor preserves the existing server validation, role enforcement, revisions and audited writes. The optional community street layer is interactive-only, attributed and limited to visible images; CI blocks third-party traffic. No production zone, fee, slot or feature-flag percentage was changed.

Visual editor source **143f2d2f9888b049a3d40093da16cd5b29b2f01c** passed Node **34490704274** and browser **34490704202**, including **23** real database journey checks. Inspection additionally found the edit-zone click dispatcher did not await its asynchronous catalog load, allowing failures to escape its error handler. The dispatcher now awaits the existing busy/error path. Its first local regression used the real common module with an incomplete DOM fixture and failed with `document is not defined`; isolating that shared UI helper corrected the fixture and the local suite now passes **171** tests. Expanded pointer-drag, street-layer-failure and PostGIS rejection checks are pending the next browser run. Production remains on 716bb7 at this checkpoint.

Visual editor source **d228324442f7fd35ff0d4fb18b8bf38e3c3dd139** passed Node **34491032253** and browser **34491032171**, including **24** full journey checks. Pointer dragging and undo preserve interior exclusions; a blocked street-image layer preserves geographic inputs; PostGIS rejects a self-intersecting polygon with HTTP 422 and leaves the saved polygon/revision unchanged. Render **dep-dahc5h6k1f9s73f940a0** became live **14:46:57 UTC**, and exact-commit production smoke **34491217550** passed. No migration or mobile source changed in this release. A read-only production check confirmed a new live owner session, one active administrator, zero direct client JANA RPC grants and unchanged stock/order fingerprints; zone fingerprint was a9f46fa52aebfef1ca56ea7c3d19fa6d. Production browser rerun 34491217551 is not claimed complete here.

The next warehouse phase will add typed inventory disposal events. Local offline Supabase CLI lookup failed with ENOTCACHED; an online registry read returned 2.117.0, but installation was interrupted when network approval was cancelled. A subsequent offline invocation found the installed CLI and returned its migration help successfully, so the proposed CI scaffold was removed before publication. CLI-generated temporary metadata is ignored. New schema changes still require disposable transaction tests before production application.

Inventory-disposal draft adds immutable typed waste/damage/supplier-return events, current lot context, revision and unreserved-stock guards, same-key retries, unique lot/kind/document references and proportional cost recognition. UI confirmation and a finance-readable history are implemented. Local JavaScript suite **175 passed** and syntax checks passed; sixteen PostgreSQL groups and the extended browser journey are pending. No disposal migration, Edge function or inventory change has been applied to production.

Disposal source **969697b98526dbff8be66e1552b69367dd91f2b1** passed Node **34492552568**, PostgreSQL **34492552637** (all sixteen new groups plus existing suites), and browser **34492552641** (**26** journey checks). Migration **20260910150043** is applied with exactly the tested SQL; its predeployment CLI filename was aligned with the actual migration record. Operations Edge **v17** is ACTIVE. Production stock/order/lot/zone fingerprints match before and after, the new disposal table has zero rows and RLS enabled, direct client table/RPC grants are zero, and deep health passes every invariant. The database advisor retains 42 intentional service-only RLS/no-policy notices and the existing Supabase-owned PostGIS findings; system extension objects were not modified. Gateway promotion is pending.

Disposal release **593c96265de0ca59ac0d86ebe2bc1471e79c123f** passed renamed-migration replay **34493495100**, browser **34493494989** and Node **34493495002** before promotion. Render **dep-dahcgtqd0e5s738skh60** became LIVE **15:11:21 UTC**. Exact-commit smoke **34493833102**, production-branch DB **34493833009** and browser **34493832928** passed. All 175 JavaScript tests and 26 browser journey checks passed on this source. No production disposal record was created for verification.

Read-only stock movement history is being implemented with stable 50-row pages, canonical filters and signed stock/reservation/cost evidence. Initial local new test failed because its expected fixture token differed from the shared fixture header; the assertion now derives the supplied token. The application token forwarding was correct. Database and browser verification for this next feature are pending. CLI migration creation returned a network-approval cancellation after producing the draft file; the existing generated file was used once and not regenerated.

Movement source d67b4b0 passed Node 34494336677, but DB **34494336806** and browser **34494336703** failed while replaying the unapplied draft migration. PostgreSQL rejected an unparenthesized CASE expression in a PL/pgSQL IF bound. The CASE expression is now parenthesized; both real-database gates must pass before migration application. Production remains on verified disposal release 593c962.

Movement correction **f21069d3b6ee90454e84698e26933fcb8a46fa18** passed Node **34494517385**, PostgreSQL **34494517407** (eight history groups plus preceding suites), and browser **34494517388** (**27** checks). The exact tested migration is applied as **20260910151830**, aligned from its CLI draft filename, and operations Edge **v18** is ACTIVE. Migration count is 76, recorded stock movements remain zero (no fabricated historical backfill), all business invariants pass, and order/stock/lot fingerprints and zero client RPC grants are unchanged. The advisor retains the documented Supabase-owned PostGIS findings. Gateway promotion and the expanded delivery-failure/retry assertion remain pending.

Movement release **0e7f8b398b8ff352b196b829883d06adfedea35d** passed Node **34494913931**, renamed-migration database **34494913909** and browser **34494913989** with **28** checks, including an actual failed-delivery/retry flow. Render **dep-dahcnk6k1f9s73fb572g** became LIVE **15:25:37 UTC**; exact-commit smoke **34495477006**, browser **34495476994** and DB **34495477001** passed. Stock movement history is now deployed.

The optional external notification outbox, lease worker, in-app repository binding and redacted monitoring page are implemented locally. Local JavaScript suite passes **185** tests. Real PostgreSQL groups and the disabled-channel browser assertion are pending; no outbox schema, worker activation, destination or external message has been created in production.

Predeployment review identified that a pending email job also needs to bind to its approved sender identity, not only provider name: switching sending domains/accounts must not restart an uncertain submission under another provider idempotency scope. The unapplied outbox draft now snapshots a nonsecret sender-scope hash; worker claims and sending reject mismatches. A dedicated database and worker regression were added. Local JavaScript suite now passes 186 tests. This revision requires its own database/browser gates before application.

Outbox source e0debfb passed Node **34496249419** and the expanded browser gate **34496249481**, but database **34496249384** failed in its first disabled-channel assertion: psql emits an empty field for SQL NULL, while the fixture decoder expected JSON text. The claim fixture now explicitly coalesces SQL NULL to JSON null. The production RPC correctly returns no job when disabled; no production migration was applied. Sender-scope source ba5320d requires this corrected fixture and another database run.

Corrected-null fixture source 49cdcaf passed Node **34496695993**; DB **34496696117** passed its lease/idempotency/sender/uncertainty groups and then failed in the rollback assertion because the test called unqualified pgcrypto digest outside the function search path. The test now checks the actual returned notification ID and confirms both notification and outbox rows rolled back, without reproducing the implementation hash. Canonical SQL functions already pin the extensions search path. No production outbox migration has been applied.

Outbox source **a292d857f164df78fdf77e297ac86429dfc4fbde** passed Node **34497013267** and PostgreSQL **34497013283**, including **18** notification groups and all preceding suites. The same SQL/Edge/browser source passed browser **34496542504** with **29** checks. After resuming, migration **20260910162639** was applied with unchanged tested SQL and operations Edge **v19** became ACTIVE. The CLI draft filename was aligned with the actual migration record. Production has **77** migrations, **46** RLS application tables, zero enabled external channels, zero destinations/outbox jobs and zero direct client JANA RPC grants. Order/stock/lot fingerprints and all business invariants remain unchanged. The advisor retains the documented Supabase-owned PostGIS findings. No sending worker was enabled. Gateway promotion is pending.


## 2026-09-10 — Outbox deployment and startup navigation regression

Checkpoint 1c779292fcaa554550084718371101f5cb0a01da passed Node 34502365341, browser 34502365342 and database 34502365242 before promotion. Render dep-dahdnm942hec73ce8qe0 became LIVE at 16:33:59 UTC. Exact-commit smoke 34502809081 and database 34502809121 passed. This deploy adds monitoring only; external channels, verified destinations and jobs remain zero/disabled.

The repeated browser run 34502809082 FAILED at account/profile navigation after reloading a real authenticated session (job 102957490063). Its retained trace shows the profile button detached and the page replaced by the storefront. Source inspection proves boot() unconditionally called renderShop() after the delayed favorites response, discarding the selected account page. This is an application race, not a reason to extend the timeout or blindly rerun.

The correction preserves the selected view, displays session-loading state before exposing account actions, deduplicates startup retries and prevents a late orders response replacing another selected page. The browser regression deliberately holds the actual disposable-database favorites response while selecting Account, then releases it and verifies the profile stays accessible without premature login UI. Local syntax and 186 JavaScript tests pass; the revised browser gate is pending.

A read-only PostHog project listing still exposes only Default project 601768, not a dedicated JANA project. No project data was queried, no flags were changed and no events were sent to it.


## 2026-09-10 — Verified startup fix and customer-return candidate

Source 7324cf25cb26b0481f1c877bedf0ef466a5fbf37 passed Node 34503441272 and the expanded 30-check browser run 34503441214. After promotion, Render dep-dahdrgjl550s73ftf8i0 became LIVE at 16:42:17 UTC; exact-commit smoke 34503631787 and browser repeat 34503631733 passed. Database/Edge source did not change for this fix; database 34502809121 remains its latest full replay. The earlier browser failure remains recorded above.

The customer-return candidate adds quarantined physical receipt, one quality decision, original-lot cost reversal, server receipt limits and a returned-shipment redispatch guard. SQL was scaffolded with the installed Supabase CLI as draft 20260910164224. The CLI created the file before its ancillary network approval was cancelled; no production operation followed that command. First local syntax validation caught an unescaped slash in the new gateway route expression; corrected before commit/deployment. Subsequent syntax and all 189 JavaScript tests pass. Real PostgreSQL concurrency and the expanded browser journey are pending. No production return fixtures exist.

Source review also found the notification menu key accidentally listed as a staff role; removed it from staff role choices. Backend role validation already rejected it. Canonical roles are unchanged.


## 2026-09-10 — Customer-return tests and applied backend

Source 86940a593b0041ab655dc125bfdb738877b78e21 passed Node 34505363265 (189 tests), database 34505363344 (22 return groups plus all existing suites), and browser 34505363317 (33 checks). Production migration 20260910170206_jana_customer_return_inspection is applied with the identical tested SQL, renamed from its draft timestamp. Customer API v25 and operations Edge v20 are ACTIVE. Gateway promotion remains pending.

The postmigration audit found 78 migrations, 48 RLS application tables, zero direct JANA client-role function grants, zero return receipts/inspections and zero business-invariant violations. Exact orders/balances/lots hashes remain d6077d5d61b0f28fd6fd2388cd4d292f / 191d727994f8e1876d9c6e3db394a91c / 40263aedf8cf425f3b8eba51fa82e806. No production stock, commercial order or cash event was created by testing or deployment.

Security advisor remains non-clean only for the unchanged Supabase-owned PostGIS findings: spatial_ref_sys RLS, public extension placement, and three st_estimatedextent overload grants to each API client role. The 48 application RLS/no-policy informational findings are intentional service-only access. No PostGIS object was modified.


## 2026-09-10 — Recovered source, verified return release, and custody extension

The recovery session found the already-tested return feature on jana-integrity at 4069c4f, ahead of jana-live. It preserved that implementation, added one regression and a gateway redaction correction, and published 0b1c63e. Node 34512867845 and browser 34512867919 passed before promotion. Render dep-dahf7teq1p3s73em6eag became live at 18:16:50 UTC. Node/exact-commit smoke 34513034338, browser 34513034290 and database 34513034278 all passed on jana-live. Public smoke recorded 0b1c63e throughout. As previously documented, API ref updates did not initiate a Render build; one manual JANA deploy was triggered only after confirming no deployment was pending.

The next proven gap is rejected-return custody: inspection records rejection but had no final-disposition document. The additive extension provides partial destruction/handover evidence, serialized quantity limits, duplicate-document protection and read-only review. SQL was scaffolded by installed Supabase CLI 2.117.0 as 20260910182225; its ancillary update network request was cancelled after file creation. Local syntax and 197 JavaScript tests pass. An initial new test expected a different fixture token; corrected to the actual fixture, with no runtime change. PostgreSQL/browser gates and production deployment of custody are pending; no production return or disposition fixtures were created.

Source 5cf7eff passed Node 34515292266 and the expanded 36-check browser run 34515292272. Database 34515292340 passed all return behavior/concurrency cases, then failed its final RLS assertion because the test fed PostgreSQL boolean text (`t`) to a JSON parser. The assertion now selects `to_jsonb(relrowsecurity)` and still requires true. Runtime SQL/Edge/UI code is unchanged; the complete database replay is being rerun before production application.


The corrected source c14af9d passed database 34515669482, including all 34 return/custody groups and the scheduled-expiration check; Node 34515669359 passed. Runtime SQL/Edge/UI is identical to the 36-check browser source 5cf7eff. Migration 20260910184237_jana_return_custody_disposition is applied with the exact tested SQL (the CLI draft filename was renamed only), and operations Edge v21 is ACTIVE. Existing orders/balances/lots hashes remain d6077d5d61b0f28fd6fd2388cd4d292f / 191d727994f8e1876d9c6e3db394a91c / 40263aedf8cf425f3b8eba51fa82e806; cash and cost ledgers also match their pre-application hashes. There are zero production return/inspection/disposition rows, zero custody overruns, RLS is enabled and new JANA client-role RPC grants remain zero. Advisors add only the expected service-only RLS/no-policy informational entry (49 application tables); all existing PostGIS findings are unchanged. Gateway promotion and public verification follow this backend checkpoint.


## 2026-09-10 — Fixed basket measurement and applied backend

Baseline custody release `09352a2` was verified live by `34516898072`; its post-promotion database `34516897968` and browser `34516897939` also succeeded.

Basket candidate `263b52f` passed Node `34519010407`. Database `34519010958` and browser `34519010367` exposed an ambiguous PL/pgSQL component alias in the new measurement lookup. No candidate code was promoted or applied to production at that stage. The alias was corrected in `d576bfc`, which passed Node `34519324896` (204 tests), database `34519324849` (all suites, including twelve basket groups) and browser `34519324900` (39 checks).

The change records every fixed-basket component in its own stock unit, including zero, shortages and excess. Measurement never changes the customer-approved basket price, reservations, usable inventory or money. Finalization requires all measured quantities to match the sold component totals. The record, audit, persistent retry and optimistic order revision are transactional. Customer-approved whole-line replacement must receive fresh component measurements; component-level replacements and variance pricing/tolerances remain pending commercial policy. See BASKET-COMPONENT-CHECKS.md.

Migration `20260910191908_jana_basket_component_checks` was applied at 19:19 UTC and aligned to its actual server version without changing the tested SQL bytes. The migration count is now 80. API v26 was deployed with its existing custom-session authentication configuration. Before/after hashes of existing order business fields, balances, lots, cash entries and cost entries matched; the new order field is revision metadata. No active multi-component picking order was present during rollout, and no production test data was created. Postmigration health returned `ok:true` with all business invariant counters zero. Direct anon/authenticated grants and service-role helper grants remain zero; orders and picking issues retain RLS. The unchanged advisor findings are the 49 intentional service-only tables plus Supabase-owned PostGIS notices. Final gateway promotion and exact-commit smoke are separate deployment gates.

## Commercial storefront and photographs — 2026-09-10

- Candidate `0ea617d766740bbca5bb4df618e9c561a67ae387`: [214 Node checks](https://github.com/NAIFMUSFER/Heavy/actions/runs/34532019904) and [42 complete browser checks](https://github.com/NAIFMUSFER/Heavy/actions/runs/34532019885) passed. The browser covers persisted merchant draft/publication, pause/reopen, public policy viewing, quote-bound policies and the full warehouse-to-customer financial journey including actual product image decoding.
- [Full PostgreSQL run](https://github.com/NAIFMUSFER/Heavy/actions/runs/34525055118) passed on `a39d47a10bc68ca82fdd6920c77bbb8b782177c7`; database and Edge source remained identical in the later browser candidate. The commercial suite adds 11 groups, alongside all previous suites. Policy versioning, quote/order terms, tax and preview gates, role isolation, persistent retry and publication/admission races passed.
- [Expo export](https://github.com/NAIFMUSFER/Heavy/actions/runs/34524731857), [Android debug](https://github.com/NAIFMUSFER/Heavy/actions/runs/34524731898) and [iOS simulator](https://github.com/NAIFMUSFER/Heavy/actions/runs/34524731779) passed on `7cc51b6d7fcc0c7080ded22f2c3ab0a501aab5fe`. The complete mobile tree was compared against the release candidate and is byte-identical. Android artifact `10171330664` and iOS artifact `10171228342` expire on 2026-09-17. No signed store release or physical-device acceptance is claimed.
- Initial database gate caught an ambiguous PL/pgSQL `CASE` in the new profile validator, corrected in `b3b75a`. A fixture attempted to edit immutable merchandise; it was corrected to create a real product version in `a39d47a`. Browser sequencing was corrected to wait for saved revisions and explicitly return to order operations. Failed runs remain visible; production migration was applied only after the corrected database and browser suites passed.
- Applied migration `20260910212709_jana_commercial_storefront`: migration history now 81 entries. The local filename is renamed to the actual Supabase timestamp with SQL unchanged. Production starts with `published:null`, `accepting_orders:false`. All eight current active offerings are known preview records; none was relabeled or converted into physical inventory.
- `jana-api` v27 and `jana-critical` v6 deployed; each entrypoint and `http.ts` was re-read and matched byte-for-byte. Custom opaque-session authentication remains unchanged (`verify_jwt:false` deliberately). `jana-ops-extra` remains v21.
- Pre/post migration fingerprints matched: orders `eee974078f5fe998574422dbbadf9314`; balances `191d727994f8e1876d9c6e3db394a91c`; lots `40263aedf8cf425f3b8eba51fa82e806`; cash and cost entries each `d751713988987e9331980363e24189ce`. No production quote, order, receipt, stock or monetary fixture was created. Health reports `ok:true` and zero stock/reservation/capacity/duplicate-order/cash violations.
- Both new tables have RLS; direct client RPC grants and service-role execution of private helpers are zero. The security advisor reports 51 INFO no-client-policy application tables (49 prior plus these two), with the same platform-owned PostGIS notices: one ERROR for the reference table, one WARN for extension placement, three anon and three authenticated extension-function WARN findings. The advisor is not reported as clean.
- Owner data, approved policies/tax status, real merchandise/stock/coverage, verified communication and recovery providers, a measured isolated production-data restore, and distribution accounts remain required. VAT-registered intake is explicitly blocked until tax/invoice integration exists. See [commercial operation notes](COMMERCIAL-STOREFRONT.md). Web publication and exact public smoke verification are separate final release gates.

## 2026-09-10 — Customer order journey candidate

Candidate `6f2421adef7238d9079a96aa9d9b08a090fff64f` adds frozen order detail/timeline, cursor paging, explicit courier GPS sharing and navigation, and all-session password rotation across web/native. Node `34541412604` passed 239 tests and Expo `34541412653` passed. Browser `34541412686` stopped on a fixture selector that expected address details to be the entire paragraph; the actual UI correctly combines city/street/details. The selector now matches the detail text within that paragraph. No candidate migration, Edge code or gateway was deployed before this correction. Remaining gate/deployment outcomes are recorded in the release handoff.

Database `34541412700` passed all suites, including eleven order/session groups. Android `34541412601` and iOS simulator `34541412639` passed on the same runtime source. Browser retry `34541602963` verified order details, courier navigation and actual foreground location sharing/ended-location privacy, then stopped because its phone-width test selected the hidden desktop navigation button. The test now selects the visible order navigation. Runtime source is unchanged; no production deployment was made on a failed browser gate.

Browser source `792d000e485d129b287e9f015a451f2ba1ff6b20` passed Node `34542145412` and browser `34542145250` with **54 checks**, including every older-order page and two-browser password invalidation/new-password login. Mobile/assets/Edge/runtime source is byte-identical to the successful database/Expo/Android/iOS source `6f2421a`; only browser fixture selectors and this evidence changed.

Migration `20260910232842_jana_order_journey_and_password_sessions` was applied after all required gates passed; its CLI draft filename was renamed without changing SQL. There are now **82 migrations**. `jana-api` **v29** is active with custom session authentication retained. All four deployed files were read back and matched the tested source. At 23:29 UTC, before/after orders/balances/lots/cash/cost fingerprints matched; business health remained zero violations, with one existing order and no courier positions inserted in production. Application RPC anon/authenticated grants remain zero. The existing 51 informational RLS/service-only findings and Supabase-owned PostGIS advisor findings are unchanged.

Android artifact `10177571268` ([run](https://github.com/NAIFMUSFER/Heavy/actions/runs/34541412601)) and iOS simulator artifact `10177526192` ([run](https://github.com/NAIFMUSFER/Heavy/actions/runs/34541412639)) expire 17 September 2026. They are debug/simulator builds, not signed distribution releases. Gateway promotion and exact public smoke are recorded in the release handoff; backend application alone is not proof of gateway rollout.

## 2026-09-11 — Populated fixture recovery candidate

At the start of this continuation, both JANA branches remained at 88139d7f8bb662c3c913903fe63f4ad0ee7d057c. All nine workflow runs for that source completed successfully, including post-promotion database/browser/native builds and exact live smoke 34542736035. Render dep-dahjt1m1egvs738bdbqg remains live on that commit. Read-only production checks returned healthy business invariants and closed store admission.

The next evidenced gap is populated backup restoration, distinct from existing empty-schema replay. The new review-branch recovery workflow creates only isolated fixtures, restores into a separate network-disabled database, compares all public table contents and application metadata, and checks restored order reads, persistent retries and new reservations. Eight pure guard/comparison tests accompany it. First CI execution is pending; no production code, schema or business data was changed. The execution environment is unavailable in this continuation, so verification uses GitHub-hosted disposable CI rather than claiming local tests.

First recovery run 34548662337 on e31bef388903a9fd387244e4284307ab63437dbc passed all eight guards, fixture creation, dump and atomic restore, then failed the structural constraint fingerprint comparison. Every public table fingerprint and the other metadata categories matched. This is not a passed rehearsal. Schema-only difference diagnostics were added without weakening the assertion to establish the exact cause. Node run 34548662269 passed. Production remains unchanged.

Diagnostic run 34548797185 on 62a892fa40b4c2df3aa28cba71db2e93a06bda7b proved that the only difference was PostgreSQL reparsing sixteen CHECK definitions: a text[] cast over an array of literal varchar values was distributed to each literal. Constraint names/types/validation, all row data and other metadata matched. The comparison now canonicalizes only that exact equivalent literal-only form; four regression tests (twelve total) cover all sixteen recorded pairs, changed values, escaped literals, idempotence and refusal to normalize other types/expressions. No live schema constraint was modified. Full recovery verification remains pending.


## 2026-09-11 — Fixture recovery gate verified

Source `278d1326673b87813696304989d808049b9d840c` passed [recovery 34548982939](https://github.com/NAIFMUSFER/Heavy/actions/runs/34548982939), [239 Node checks 34548982909](https://github.com/NAIFMUSFER/Heavy/actions/runs/34548982909) and the [complete PostgreSQL gate 34548982873](https://github.com/NAIFMUSFER/Heavy/actions/runs/34548982873). Earlier candidate database gates 34548662340 and 34548797120 also passed. The two failed recovery attempts above remain part of the evidence, not hidden or reported as passes.

At 01:02:46 UTC (04:02:46 Riyadh), recovery reported twelve guard/normalization tests and eleven successful integration groups, with matching contents across 52 public tables (24 nonempty), application RLS/privileges/owners, canonical constraint definitions, triggers, indexes, extensions and saved scheduler definitions. The recovered custom session read the same order, persisted confirmation retry did not duplicate business records, and a new quote could reserve stock with healthy invariants. There are zero public sequences in the current schema: the gate checked that both sets remained empty, not a populated sequence recovery.

Temporary fixture archive: 629,321 bytes, SHA-256 `410dfc2f588a7026e77b3c66a468b87f91ab470722bd7903762f541f7f131165`; dump 0.264 seconds, restore 0.552 seconds, measured recovery cycle 12.670 seconds excluding fixture/bootstrap setup. These are fixture-only measurements and **not production RPO/RTO or load evidence**. Only the aggregate [evidence artifact](https://github.com/NAIFMUSFER/Heavy/actions/runs/34548982939/artifacts/10180088950) was uploaded. The archive and disposable containers/volumes were removed.

The change is test infrastructure and documentation on `jana-integrity` only. No gateway/mobile/Edge runtime or SQL migration changed, and no production deployment, data fixture, admission change or provider message was performed. `jana-live` remains at the previously verified 88139d7 release; no redundant rollout is needed for this review-branch test addition. Documentation after this checkpoint does not change the tested recovery source. Real isolated production-data restore, storage/provider configuration recovery, approved retention/objectives and physical operating acceptance remain outstanding.

## 11 September 2026 — staff order paging candidate

Started from review `c32999ecfa3a6906dbb919f14fe9c1a2cdd71539` and live `88139d7f8bb662c3c913903fe63f4ad0ee7d057c`; latest Render deployment was still `dep-dahjt1m1egvs738bdbqg`. Source inspection proved operations UI fetched only the first 100 orders with no continuation, while Edge loaded all permitted rows before slicing.

The candidate adds a service-only bounded staff paging RPC and indexes, routes Edge to that RPC, and adds retained/deduplicated/retryable history with refresh/session request guards. Legacy list RPCs and offset clients remain compatible; existing picker/courier assignment and delivered cash-task visibility are preserved. No business data rewrite or native source change is included. Added disposable database role/cursor/cash-journey checks and real browser phone-width pagination/failure-retry evidence. CI and deployment evidence will be appended after verification; this paragraph is not a production release claim.

Candidate `b560a9756f7475f94fa6ec93d38dee8bfee0323e`: verification run `34551883225` passed all 245 Node tests. Database run `34551883103` replayed the migration and passed >100/tied-time/new-arrival paging, then the new disposable staff fixture failed because its required `verified_phone=false` value was omitted. The fixture is corrected; no production code or schema change was needed for this test failure. Added actual PostgreSQL RPC signature checks for both cursor and legacy staff requests.

The same candidate recovery run `34551883115` restored all rows but stopped at index metadata comparison; the new partial-index predicate uses the already-documented PostgreSQL varchar-literal-array cast representation. The existing narrowly scoped normalization is extended to index definitions with a regression ensuring column, ordering and predicate changes still fail, plus exact schema-only difference diagnostics. Browser run `34551883140` passed the real existing journeys and customer history beyond 100, then the new test tried to close an already-closed admin dialog; the test now checks whether it is open. All affected gates must pass before deployment.


### Validated staff paging source and backend deployment

Candidate `40e0780470e9f096ebf2d8d7b53b959b273b4f75` passed every relevant gate:

- Node `34552136765`: 245 tests passed.
- Database `34552136776`: full migration replay, RPC contracts and transactional/concurrency suites, including the new staff paging/role/delivery cash checks.
- Browser `34552136742`: 56 real gateway/Edge/PostgreSQL checks, including staff phone-width >100 history, retained cards after a 503 page failure, successful retry, no duplicates/overflow, and refresh to the first 50.
- Recovery `34552136730`: 13 normalization/guard tests and 11 integration groups; 52 public tables (24 nonempty) restored with identical fingerprints. Schema diagnostics proved the sole raw index difference was the equivalent varchar-literal-array cast distribution in `jana_orders_picker_page_idx`. Columns, ordering, index predicate values and grants still must match.

On 2026-09-11 at 01:51–01:52 UTC the approved Supabase project applied migration `20260911015144_jana_operations_order_paging` (83 total), and `jana-api` v30 deployed ACTIVE with `verify_jwt:false`. All four deployed Edge source files are read from the tested candidate. `jana-critical` v6 and `jana-ops-extra` v21 were not redeployed. The CLI-scaffolded migration file is renamed to the actual recorded application version; SQL bytes are unchanged.

Post-migration checks found all three paging indexes valid, execution restricted to service_role, every stock/slot/cash/duplicate-order invariant zero, the original one production order and zero courier positions. Storefront remains unpublished and order admission closed. No customer, order, stock or cash fixtures were written to production. Web promotion and deployment verification follow; this entry does not yet claim the new web is live.


### Staff paging production verification — 11 September 2026

**Live application:** `ca1f650b7d7cdff9577cb3aace0c8246d965ed32`, Render `dep-dahm129594qs73fj9h70`, LIVE at `2026-09-11T01:59:59.647751Z`. The final source (including exact migration history alignment) passed review Node `34552492111`, database `34552492099`, browser `34552492160`, and recovery `34552492059`. Its application/Edge bytes match candidate `40e0780`; the migration rename has identical SQL bytes. Browser artifact `10181342105` contains the disposable phone-width staff pagination evidence.

Production-branch verification also passed Node/live smoke `34552678670`, database `34552678741`, browser `34552678734`, and recovery `34552678709`. Live-smoke job `103118682122` reported `ok:true`, the exact live SHA, and 23 checks at `2026-09-11T02:00:14.8574653Z`: commit stability, dependency health, public UI/assets/CSP, catalog/storefront, authentication rejection and private-source blocking. All four retrieved deployed jana-api v30 files match tested source byte-for-byte. No native source changed, native build rerun or signed mobile release was produced.

Render did not automatically deploy the branch promotion. Three observations found no new deploy or build; the previous build log states it lacks repository access and clones the public URL. Render documents that public-URL services require manual deployment even though this service reports `autoDeploy:yes`. After confirming the exact branch and no active deploy, one API deploy was triggered at 01:59:37 UTC. No duplicate deploy or service-setting change was made. See the operating runbook before assuming an automatic webhook exists.

At 02:01 UTC, post-deploy invariant checks were healthy: negative/reserved stock, overbooked slots, duplicate orders and cash/ledger mismatches all zero; production still has its original one order, zero courier positions, 8 preview catalog entries and 14 active slots. Storefront remains unpublished with intake closed. No error-level Render log appeared in the deployment window 01:59:37–02:01:00 UTC. Security advisors remained unchanged: 51 expected service-only RLS INFO findings and the existing platform-owned PostGIS findings; the new RPC is inaccessible to anon/authenticated and executable by service_role.

Remaining commercial prerequisites are unchanged: actual merchant/catalog/prices/inspected stock/geography/capacity, field acceptance, approved verification/communication providers, dedicated staging/load/real-backup recovery evidence, tax/invoicing where applicable, and signed store distribution/physical devices. Multiwarehouse segregation/routing, approved basket component variance/substitution rules and supplier credit reconciliation remain explicit work items. A docs-only review checkpoint records this result without causing another application deployment.


## Checkout recovery candidate — 11 September 2026

Source adds one backward-compatible, read-only quote-detail migration and a shared web/Expo recovery controller. Previously the web wrote an unused pending quote ID and native kept the quote only in component state. The candidate persists an account-scoped reference before confirmation, resumes its current server outcome, disables expired/unknown confirmation, exposes explicit quote release, and keeps an existing order when cancellation loses the race. Server clock anchoring avoids incorrect device wall-clock expiry. Changed carts are retained; reference acknowledgement follows successful cart persistence.

Local verification: gateway/browser syntax checks and **264 Node tests** passed (19 new shared checkout/transport tests). Added disposable database tests and real browser restart/expiry/lost-response scenarios await candidate CI. Android, iOS simulator and Expo export must run for this new mobile source. No database migration or web deployment is claimed for this candidate yet. The production baseline remains ca1f650 / dep-dahm129594qs73fj9h70, 83 migrations and jana-api v30 until subsequent evidence records a completed promotion. No production business fixture was created.


Candidate **0b768e822e296210b5b2881797b8244d26dfacf3** passed verification **34555272158** (264 Node tests), Expo export **34555272209**, populated recovery **34555272198**, and browser commerce **34555272205** (59 checks, including real reload/expiry/response-loss recovery). Android **34555272151** and iOS **34555272154** were still building at this checkpoint. Database **34555272150** stopped in the new test after its first successful read-only/stock assertion: `psql` emits boolean `f`, while the helper requires JSON `false`. The privilege queries now explicitly return `to_jsonb(...)`; this corrects the test parser without changing privileges or application code. The database gate must pass before applying the migration.


The initial Android **34555272151** and iOS simulator **34555272154** builds completed successfully. Subsequent review reproduced a native retry-journal race with two failing tests: an old successful response replaced a newer account's pending journal, or recreated the removed journal after logout. Cleanup now leaves an absent/different-session journal untouched. Both regressions and all 32 targeted checkout/transport tests pass, and the complete local suite is **266 passed**. A genuinely changed mobile API requires fresh native/export gates; the previous build results do not stand in for them. The controller logout test also now pauses an actual in-flight storage write and proves that logout waits for its removal without submitting an order.


Database correction **b2ac101f4b5d038a5d885434eb2c6e214a511552** passed full database **34555542993**, including all **7** new checkout recovery groups, and verification **34555542968**. Mobile retry correction **49e34f34e2b4547aa1f75e553de6b6f3de7d8b10** passed verification **34555759955** (266 tests) and Expo export **34555759966**; Android **34555759985** and iOS **34555759935** are the new required native builds. Browser and database application source are byte-identical to the successful corresponding gates.

At **2026-09-11 02:45 UTC**, the exact tested migration was applied to Supabase jjdsajiwoqanefmnikls as **20260911024522_jana_checkout_recovery** (84 total). The CLI-generated file is renamed to the recorded application timestamp without changing SQL bytes. This introduces only the read-only quote recovery fields; no Edge deployment is needed (jana-api v30, jana-critical v6, jana-ops-extra v21 remain ACTIVE with verify_jwt:false).

Post-application evidence: the existing **1 order / 0 courier positions** are unchanged; hashes of complete order, stock-balance, inventory-lot and delivery-slot records are identical before/after. Deep health is true with all reservation, slot, duplicate-order, cash and ledger violations zero. The quote reader remains non-executable by anon/authenticated and executable by service_role; its installed definition includes server_now. Advisers retain the prior 51 intentional service-only RLS INFO findings and the same platform-owned PostGIS findings (1 public spatial-reference RLS error, 1 extension-placement warning and 3 function-execute warnings per client role). No new application grants or business data were created. Web remains ca1f650 until the aligned release completes gates and promotion.


## Checkout candidate complete; Render billing suspension — 11 September 2026

Release candidate **98eb36f118c62779ee56ff6f2772e816d2b14f7a** passed all applicable review gates: verification **34555982230** (266 Node tests), database **34555982242** (including 7 checkout groups), populated recovery **34555982225**, and browser **34555982297** (59 checks). Latest native-source commit **49e34f34e2b4547aa1f75e553de6b6f3de7d8b10** passed Expo **34555759966**, Android **34555759985**, and iOS simulator **34555759935**. The complete mobile source tree is identical in both commits: `7cb78fccce6cf24324793f55b98fd6151eceb0ca`.

Artifacts: Android debug **10182635544** (SHA-256 `61c740f7290930c66d490b0b2ee2ea726b9f5a9c6d03644f9728c071e8c85b78`, expires September 18 02:50 UTC); iOS simulator **10182597665** (`ed65c98a44275b790ecd6614c2b589ea594f65e818814720008ee4cacc9c3732`, expires September 18 02:49 UTC); browser evidence **10182589050** (`46f635fe117799e45c44bbcc043b426ba9d5f320d5a24642fff29cc8d640e2c0`, expires September 25 02:48 UTC). The browser artifact includes the phone-width recovered-order screen. These mobile packages are debug/simulator build evidence, not App Store/Play distribution or physical-device acceptance.

**Web promotion is blocked by the hosting account, not completed.** Two fresh service reads around 02:48–02:50 UTC returned `suspended: suspended`, `suspenders: [billing]` for exactly srv-dagnferl550s73cbivhg in tea-d9qth1iju40c73btab90; its service record was last updated at 02:33:21 UTC. The configured instance plan is free. A filtered suspension/billing log query returned no additional explanation, so no invoice amount, exhausted allowance or payment failure is inferred. The historical deployment dep-dahm129594qs73fj9h70 still carries a `live` label for ca1f650; that historical deployment label is not proof that a suspended service is serving traffic.

The production branch stays **ca1f650b7d7cdff9577cb3aace0c8246d965ed32**. No deployment was triggered, no hosting plan changed and no post-deployment smoke is claimed. Supabase's additive migration **20260911024522** is already applied and compatible with that existing web source; its preserved-data/grant/health evidence is above. The owner must resolve the billing suspension in the [JANA Render service/workspace](https://dashboard.render.com/web/srv-dagnferl550s73cbivhg). [Render's free-service documentation](https://render.com/docs/free#monthly-usage-limits) describes distinct billing/usage suspension causes; the specific account cause needs the owner's Billing view.

After suspension is cleared, re-read the latest branch heads, CI and service state to preserve any concurrent changes. Promote the latest reviewed compatible source, observe whether an automatic deployment starts, issue one manual deploy only if the documented public-clone path still requires it and none is pending, then verify the actual commit and public smoke. Keep commercial admission closed until the real merchant/catalog/stock/operating prerequisites pass. Independent source/backend work can continue while hosting is unavailable; this is not completion of the overall commercial-launch matrix.

## Checkout recovery published after hosting resumed — 11 September 2026

The owner reported that the invoice was paid. Fresh reads confirmed that the scoped Render service was `not_suspended`, with no suspenders; its service record was updated at **2026-09-11T02:57:28.557056Z**. This is service-state evidence, not an independent review of the invoice or payment transaction. The service remains on its existing free instance plan, with the same repository, branch and workspace.

Before promotion, `jana-integrity` was **07125e5cbcf1f383c9535fc940b74b506f081b1e** and `jana-live` remained **ca1f650b7d7cdff9577cb3aace0c8246d965ed32**. Fresh CI reads confirmed the completed review gates above and verification **34556408050**. The 07125e5 diff from tested candidate 98eb36f contains only three documentation files; its complete mobile tree remains `7cb78fccce6cf24324793f55b98fd6151eceb0ca`, identical to the successful native build source. Supabase still has 84 migrations, latest **20260911024522**, and ACTIVE jana-api v30 / jana-critical v6 / jana-ops-extra v21, all retaining `verify_jwt:false`. The installed quote reader definition hash remains `9bc58ca06986ab8f8cb5f3e9dc9e7c91`.

At **03:03:43 UTC**, the tested commit was fast-forwarded to `jana-live` without force. Two post-promotion deployment reads found no automatic/pending deployment; the prior build log again confirmed the documented public Git clone configuration. One API deployment was therefore requested at **03:04:30 UTC**, without clearing cache or changing service settings. **dep-dahmvfid0e5s7382utpg** resolved commit **07125e5cbcf1f383c9535fc940b74b506f081b1e** and became LIVE at **2026-09-11T03:05:05.590029Z**. No duplicate deploy was created.

Public verification [**34556999274**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34556999274), job **103131697953**, completed successfully. At **2026-09-11T03:05:20.1918653Z**, the smoke reported `ok:true`, **23 checks**, and the exact released commit: version stability, health/readiness, public pages/assets/CSP, catalog/storefront, anonymous authentication rejection and blocked private source. During rollout it polled only version/readiness, then ran the complete smoke once. This is actual post-deployment evidence, separate from the review-branch tests.

Production-branch verification also passed its Node gate, database **34556999271**, Expo export **34556999374**, browser **34556999339** and recovery **34556999279**. Android **34556999338** and iOS simulator **34556999299** were still running at the 03:08 UTC checkpoint; their previously completed review gates on identical execution source remain recorded above. No new passing result is inferred for an unfinished run.

Read-only post-deployment checks at **03:05 UTC** returned healthy stock/reservation/slot/duplicate-order/cash/ledger invariants, the existing **1 order / 0 courier positions**, 8 preview catalog entries and 14 active slots. Store admission remains **closed** and no merchant profile is published. No error-level Render log was returned for **03:04:30–03:05:22 UTC**. This continuation deployed the prepared web source and updated documentation; it did not reapply the migration, redeploy Edge or create/change production orders, stock, money or users.

The hosting suspension is resolved. Commercial launch still requires actual merchant identity/tax status/policies, approved catalog/prices/photographs and inspected inventory/capacity/geography, operating acceptance, approved verification/communication providers, dedicated staging/load/real-backup recovery evidence and physical-device acceptance/signed store distribution. Multiwarehouse ownership/routing, approved basket component rules and supplier credit reconciliation remain tracked gaps. A documentation-only review checkpoint records this result without triggering another production deployment.

## Cross-application resilience candidate — 11 September 2026

Started from review bcee4a9 and live 07125e5 with all prior production gates successful, service resumed, and unchanged 84-migration/custom-session Edge state. Review reproduced a web render failure from malformed saved carts, incompatible Arabic amount entry, native rounding of excessive refund precision, and broken inventory/finance/support workspace links. The candidate replaces local cart persistence with a shared validated, serialized controller, adds error recovery and account retry, uses exact localized monetary inputs and corrects role links. Static module availability, offline static dependencies, API reference and historical/current documentation are updated. Details and explicit coverage limits are in REVIEW-2026-09-11.md.

Local syntax checks and 283 Node tests passed before final integration review; targeted follow-ups cover the final shared-model/static changes. Added disposable browser cases cover phone-width corrupted-cart recovery, quota failure/retry/reload, a real inventory workspace link and Arabic decimal receipt cost. This is written/local-test evidence; candidate browser/Expo/native CI and production deployment are not yet claimed. No SQL migration, Edge deployment, provider activation or production commerce data change is included.


Candidate **f1cf8d987c3bf49b10464a74f92f9450628dcdf8** passed Node verification **34558812256** (283 tests) and Expo export **34558812345**. Browser **34558812251** completed disposable database setup but stopped before exercising the UI: a new local-cart fixture variable reused the existing `savedCart` name in the same block. Renamed the fixture variable and added browser-script syntax checking to the early Node gate. This was an invalid test script, not a passing browser result. Native builds **34558812270** / **34558812260** remain attached to the unchanged mobile source. No production promotion occurred.


Final integration review added a failing test proving that quantity-limit errors were incorrectly reported as storage failures by the new cart queue. Storage exceptions are now classified only at the actual read/write boundary; a stock/quantity validation failure retains its own message and does not suggest resetting a valid cart. The complete shared-cart tests pass after this correction. Because the shared native runtime changed, fresh Expo/Android/iOS builds are required for this corrected source.


Browser syntax correction **59befa6c339c9092458ffb5d9ff4823e4813cf40** passed verification **34559021002** and browser **34559021006**, job **103137709702**: **63** complete gateway/Edge/PostgreSQL checks at 03:37:03 UTC, including all four new workspace/localized-cost/cart-failure checks. Final shared-cart error-classification changes still require the following candidate gates; the failed initial run remains recorded above.

## Cross-application review verified and published — 11 September 2026

Final source **0a4f992e845275523d2973f49480c7620066a0f4**, tree `d9f3dfdbaa5678ea063c0975347ec16adebe2e84`, passed every applicable review gate before promotion:

| Gate | Exact run | Observed result |
|---|---|---|
| Node verification | [34559209960](https://github.com/NAIFMUSFER/Heavy/actions/runs/34559209960), job 103138278960 | 284 passed, zero failed at 03:38:41 UTC; shared/cart/browser-script syntax checks also passed |
| Disposable browser commerce | [34559209951](https://github.com/NAIFMUSFER/Heavy/actions/runs/34559209951), job 103138278493 | 63 checks complete at 03:40:13 UTC; all seven roles through real gateway/Edge/disposable PostgreSQL |
| Expo export | [34559209937](https://github.com/NAIFMUSFER/Heavy/actions/runs/34559209937) | Success on the same final source |
| Android debug | [34559209938](https://github.com/NAIFMUSFER/Heavy/actions/runs/34559209938) | Successful APK build and artifact verification |
| iOS simulator | [34559209950](https://github.com/NAIFMUSFER/Heavy/actions/runs/34559209950) | Successful simulator compilation and artifact verification |

Retained review artifacts: Android **10183786766**, SHA-256 `2e157c28e67344afa39a5daf858fa9ebd97ae8dfceb1824d8e442249924f8c70`, expires **September 18 03:42:59 UTC**; iOS simulator **10183794434**, `5317a01a21f71170402f2438f1fc0e81455131283a9fd6005f058622f5222e45`, expires **September 18 03:43:21 UTC**; browser evidence **10183726525**, `84bc14a8ff9c347f5ee0e470b3ddc574d14508f3823d997175d0340c64f779ed`, expires **September 25 03:40:13 UTC**. The browser artifact includes `cart-recovery-phone.png`. Artifacts are not signed store distribution or physical-device acceptance.

This batch corrected confirmed stored-cart render failures, asynchronous persistence/checkout ordering, localized monetary precision, native account-retry behavior and staff workspace navigation. It also reconciled API/architecture/mobile and dated feature documents. The initial browser fixture syntax failure and subsequent failed quantity-message test are documented above; their corrected final source is the source that passed these gates. Read-only path review resolved **33 relative documentation links and 57 relative source imports** without missing targets. Full coverage and limits are in [REVIEW-2026-09-11.md](REVIEW-2026-09-11.md).

Fresh pre-promotion reads confirmed `jana-integrity` at 0a4f992, `jana-live` still 07125e5 and the scoped Render service `not_suspended` on its existing free plan. At **03:45:02 UTC**, the tested source was fast-forwarded to `jana-live` without force. Two subsequent deployment reads found no automatic or pending deploy. The documented public Git clone configuration therefore required **one** API deployment, requested at **03:45:39 UTC** without clearing cache or changing settings. **dep-dahniorm8hqs73cji0cg** resolved the exact **0a4f992e845275523d2973f49480c7620066a0f4** and became LIVE at **2026-09-11T03:46:16.549455Z**. No duplicate deployment was created.

Public verification [**34559615482**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34559615482), job **103139502595**, reported `ok:true`, **26 checks** and the exact commit at **2026-09-11T03:46:28.9194501Z**. It waited for version/readiness during rollout and then ran the full smoke once, including the checkout/input/local-cart modules. This is post-deployment evidence, separate from the review tests. The production branch's Node verification also succeeded. Its push automatically starts the other applicable browser/mobile workflows; no unchanged gate was manually retriggered.

Read-only production health at **03:46:51 UTC** returned `ok:true`, zero negative-stock/reservation-overrun/slot-overbooking/duplicate-order/cash-ledger violations, **1 existing order / 0 courier positions**, 8 active preview entries and 14 active slots. Admission remains **closed**, with **no published merchant profile**. No error-level Render logs were returned for **03:45:39–03:47:17 UTC**. No SQL, Edge runtime, production business record, provider flag or hosting plan was changed. Supabase remains at the already-verified **84 migrations**, latest **20260911024522**, with jana-api v30 / jana-critical v6 / jana-ops-extra v21 and the existing `verify_jwt:false` custom-session boundary.

Remaining commercial prerequisites are actual merchant identity/contacts/tax status and policies; approved products/prices/photos and inspected inventory; actual geography/capacity/staff acceptance; approved verification/recovery/communication providers and worker/domain; dedicated staging, capacity/alerts and isolated real-production-backup recovery; physical Android/iPhone acceptance and signed store accounts/releases. Warehouse ownership/routing, approved basket component variance/substitution/pricing rules and supplier credit reconciliation remain separate work items. This documentation checkpoint is retained on `jana-integrity`; it does not trigger a second web deployment.

## Supplier credit reconciliation candidate — 11 September 2026

Started from review **0d3f841076294c4139499a6445c6a0979f398eaf** and production **0a4f992e845275523d2973f49480c7620066a0f4**, with Render LIVE on `dep-dahniorm8hqs73cji0cg`, 84 applied migrations and ACTIVE jana-api v30 / jana-critical v6 / jana-ops-extra v21. The highest independent warehouse/finance gap was proved in the existing flow: a physical `supplier_return` recorded stock and cost evidence, while finance had no transaction or UI for the supplier's later credit-note document.

The candidate adds service-only, RLS-protected, immutable supplier credit notes; strict supplier-return linkage, supplier/document uniqueness, durable idempotency and audit; stable reconciliation pages; admin/finance recording and inventory read-only review. The comparison to removed inventory cost is explicitly reference evidence, not inferred accounting or cash. The real browser fixture records an Arabic-decimal credit note and verifies stock, inventory-cost and cash ledgers do not change. Node syntax and 287 tests pass locally. Disposable database/browser CI, exact source IDs, migration/Edge application and deployment are not yet claimed in this candidate checkpoint.

Final review source **5e23306d5b73bc35347719a61fc11b6023fd765a** passed verification [**34565854723**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34565854723) (**288 Node tests**), database [**34565854730**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34565854730), browser [**34565854759**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34565854759) (**64 checks**) and populated recovery [**34565854767**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34565854767). The database gate includes concurrency, append-only enforcement, supplier/document isolation, known/unknown cost, complete keyset traversal and role/grant checks. The browser used isolated fixtures through the real gateway, Edge handlers and PostgreSQL; it created no production business data.

At **2026-09-11 05:22–05:27 UTC**, Supabase applied **20260911052244_jana_supplier_credit_reconciliation** and **20260911052752_jana_supplier_credit_actor_index** (**86 total migrations**). `jana-ops-extra` v22 is ACTIVE and retains the intentional `verify_jwt:false` custom-session boundary; jana-api v30 and jana-critical v6 are unchanged. Post-application checks found zero production supplier-credit rows, RLS enabled, zero anon/authenticated execution grants, two service-only application RPC grants and healthy deep invariants. Complete hashes/counts for the existing order, stock balances, lots, movements, inventory costs, cash, disposals and audit log were identical before/after. The follow-up index returned the unindexed-foreign-key advisor to its prior count without changing ledger rows. Web promotion is intentionally not claimed at this checkpoint.

## Supplier credit reconciliation published — 11 September 2026

The migration-aligned release **67d03047b191bf8edc8cdee24be5c038ed87166c** passed verification [**34566131144**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34566131144), database [**34566131206**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34566131206), browser [**34566131198**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34566131198) and populated recovery [**34566131130**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34566131130). It was fast-forwarded to `jana-live` without force. Render again produced no automatic deployment after two observations of the documented public-clone service, so one API deployment was requested without clearing cache or changing settings. **dep-dahp529594qs7380058g** resolved the exact release and became LIVE at **2026-09-11T05:33:23.737690Z**.

Public verification [**34566340605**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34566340605), job **103159154097**, reported `ok:true`, **26 checks** and the exact deployed commit at **05:33:34 UTC**. Read-only post-deployment checks retained the existing 1 order, 0 courier positions and zero supplier credit notes; deep stock, reservation, slot, duplicate-order, cash and ledger invariants remained healthy. Store admission is still closed and no merchant profile is published. No error-level Render logs appeared in the deployment window. No production order, stock, cash, supplier document or user record was created or changed by verification.

This release completes the software trail for immutable supplier credit-note evidence and reference-cost reconciliation. It does not certify actual supplier settlement, VAT/accounting classification or bank/ERP posting. Those procedures and approval thresholds require owner/finance acceptance with real documents; the other commercial-launch inputs in AUDIT-MATRIX.md remain outstanding.

## Isolated capacity candidate — 11 September 2026

Started from review **44fb1774c383603b66c983de511e5ce08d5d1d08** and published web **67d03047b191bf8edc8cdee24be5c038ed87166c** after confirming the prior gates and deployment remained successful. The candidate adds a separate disposable capacity workflow that runs the real local gateway, all canonical Edge handlers, PostgREST and the fully migrated PostgreSQL/PostGIS fixture. It measures 320 catalog reads at concurrency 32, asserts a generous p95/throughput regression floor, proves structured overload shedding at the existing 100-active-request boundary, and requires a successful recovery read.

The harness extension is inert unless the capacity script explicitly applies a local artificial delay. The regular browser journey retains its prior behavior. Syntax and Node regression results are local evidence only at this checkpoint; the new capacity workflow and unchanged browser journey require candidate CI. The test is read-only after fixture setup and is hard-guarded to the disposable loopback database. It does not target production, create production records or claim a production SLA. Exact candidate and run evidence will be recorded after CI resolves.

Initial capacity run **34567899663** completed fixture replay and started the isolated services, then the test's network guard rejected its own loopback gateway request before measuring load. The unchanged browser path passed **34567899510** with 64 checks, and Node verification **34567899730** passed 288 tests. The correction exposes a narrowly checked loopback-only requester while the global bridge continues to reject every origin except the dedicated emulated Supabase project. No performance result is inferred from the failed run; fresh capacity CI is required.

Corrected source **530c045e1d3dd3e1865c6973c8edfee8a77cb2d2** passed verification [**34568077980**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34568077980) (**288 Node tests**) and the unchanged browser journey [**34568077967**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34568077967) (**64 checks**). The new capacity gate [**34568077986**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34568077986), job **103164199642**, passed at **2026-09-11 06:00 UTC**: 320 read-only catalog requests at concurrency 32 completed in 582.8 ms (**549.1 req/s**, p50 **41.1 ms**, p95 **169.7 ms**, p99 **260.9 ms**). The held 125-request burst admitted 100, returned structured `503 BUSY` for 25, and recovered on the next catalog read. Aggregate artifact **10186745050**, digest `sha256:17c6352a86eec57ea5a2f5bb22d8b6c30794e87bfb1220c5b63f18b25fdf8810`, expires 25 September 2026.

This is a fixture-only single-runner regression, not a production capacity number. It makes no web, mobile, SQL or Edge runtime change, so `jana-live` intentionally remains **67d03047b191bf8edc8cdee24be5c038ed87166c** and no Render/Supabase deployment was requested. Owner-approved dedicated staging, representative managed-service/device-network load, alerts and agreed production thresholds remain required. No production record or credential was accessed by this gate.

## Customer-approved unavailable-line removal candidate — 11 September 2026

Starting after the isolated capacity checkpoint and production `67d03047`, review confirmed that a picker could replace an unavailable sellable line but could not ask the customer to remove that line with a frozen lower total. The candidate adds a distinct removal proposal for multi-line orders, blocks removal of the final line, preserves the sold line and every reservation until explicit consent, and atomically reallocates only the remaining lines after approval. Rejection or expiry retains an unresolved picker task. Customer web/native views name the action, item, previous total, exact negative difference and approval total; public history distinguishes proposal, approval, rejection and expiry.

Local syntax, gateway bundle checks and **290 Node tests** pass, including Edge allowlisting, unsafe-total consent disabling, picker controls and shared customer timeline labels. Disposable PostgreSQL tests add proposal immutability, sixteen concurrent approval retries, inventory/cost consumption, customer history, rejection and last-line protection. Initial database run **34573180735** passed the transaction through concurrent acceptance, then failed because its assertion parsed PostgreSQL `t` text as JSON; the assertion now explicitly serializes that boolean and requires a fresh database result. Browser, recovery, capacity, verification and Expo gates passed the initial source; native and corrected database results, migration application, Edge deployment and public release are not yet claimed at this candidate checkpoint.

Corrected candidate **eb256f8a5e9707b3d5d2f3c8ce1016235e3a8c74** passed [verification **34573375647**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34573375647) (**290 Node tests**) and the complete [PostgreSQL gate **34573375641**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34573375641). The substitution suite passed **21 groups**, including the five whole-line removal groups, sixteen concurrent approval retries, last-line protection and private helper boundaries. Its application, migration, Edge and mobile trees are identical to initial feature source **3e3c59b3ac39a0e9438ae9e2716501c5f11c4e5f**; only the test serialization and this evidence changed. That source passed [browser **34573181145**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34573181145) (**64 checks**), [recovery **34573180844**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34573180844), [capacity **34573180859**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34573180859), [Expo **34573181184**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34573181184), [Android **34573180715**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34573180715) and [iOS simulator **34573180929**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34573180929). Android artifact **10188743071** and iOS simulator artifact **10188665664** expire 18 September 2026. These are reproducible debug/simulator builds, not signed store packages or physical-device acceptance. Production remains on **67d03047b191bf8edc8cdee24be5c038ed87166c**, 86 migrations and `jana-api` v30 until a separately recorded promotion.

## Customer-approved unavailable-line removal published — 11 September 2026

Evidence-aligned release **85a4511d1ee40b9b4d38f014f00b5e45f1277cc7** passed final review verification [**34574033864**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574033864) and was fast-forwarded to `jana-live` without force. Supabase applied **20260911072254_jana_customer_approved_line_removal** (87 migrations) and deployed ACTIVE `jana-api` **v31** from the tested four-file source with the intentional custom-session `verify_jwt:false` setting retained. Direct `anon`/`authenticated` execution across the touched application RPCs remains zero; the service role may call the two public entry points but not the private reallocation helper. The new state constraint is installed and there are zero pending production removal proposals.

The service produced no automatic deployment after two observations of the branch update, matching its documented public-clone behavior, so one API deployment was requested without clearing cache or changing settings. Render **dep-dahqq4h594qs73870gs0** resolved the exact release and became LIVE at **2026-09-11T07:26:38.521573Z**. Exact-commit public smoke [**34574309224**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574309224), job **103183247319**, passed **26 checks** at **07:26:47 UTC**; production [browser **34574309355**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574309355), [database **34574309342**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574309342), [recovery **34574309168**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574309168), [capacity **34574309189**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574309189), [Expo **34574309197**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574309197), [Android **34574309196**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574309196) and [iOS simulator **34574309163**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34574309163) gates also passed on the exact source. The release-branch Android artifact **10189186141** and iOS simulator artifact **10189128740** expire 18 September 2026. `/version`, `/health` and `/ready` returned the exact commit, healthy gateway and all three healthy Edge dependencies. No error-level Render log appeared in the deployment window.

Read-only pre/post application fingerprints match exactly: orders `b88c8107c85ae02b015cca8e253817d7`; stock balances `3de03ef85495c9883ffdf88fd64ce901`; lots `9b4a8484fe19e8b2f161198097655c08`; slots `a0aaef74bd882f6b1cf8cbe9261b1882`; substitutions, picking issues, stock movements and inventory-cost entries remain empty. Deep health reports `ok:true` with zero stock, reservation, capacity, duplicate-order, cash or ledger violations. No production order, inventory, cost, cash, customer decision or fixture was created or changed. The advisor inventory remains the same five documented platform/service-only security categories and two informational performance categories; the migration added no new advisory category.

## Exact basket component replacement candidate — 11 September 2026

Review started from `3fa8d6ec1037337b16129e5dedf43c7f452c1c6b` with production still at `85a4511d1ee40b9b4d38f014f00b5e45f1277cc7`, Render LIVE on `dep-dahqq4h594qs73870gs0`, 87 migrations and ACTIVE `jana-api` v31 / `jana-critical` v6 / `jana-ops-extra` v22. The selected gap was the fixed-basket component shortage: existing measurement correctly blocked finishing, but only whole-line replacement/removal was possible.

The candidate allows a same-unit, exact-quantity component replacement at the unchanged fixed-basket price after actual shortage evidence. Proposal/rejection/expiry preserve the original snapshot, reservations and money. Explicit consent atomically rebuilds reservations, writes distinct immutable history and forces fresh measurement of the approved component set before finishing. The picker UI lists only active, sufficiently sellable stock not already present in the basket; PostgreSQL remains authoritative. Customer web/native screens name both components, quantity, unit, zero price difference and consent consequence.

Local syntax checks, `npm run check` and **293 Node tests** pass. Disposable database additions cover pre-consent immutability, 16 concurrent decision retries, exact reservation/consumption, rejection, expiry, invalid unit/component choices and execute grants. Full CI, migration/Edge application and web publication remain pending; no production data or runtime has changed at this checkpoint.

Initial CI on `f892c766f5ac5a8d5192233ce83288613e3fa814` passed Node/Expo, recovery and capacity, while database run `34577477703` exposed a regression in the redefined legacy whole-line decision: its accepted issue was not marked replaced, so finalization remained blocked. Browser run `34577477579` reached the fixed-basket journey and found that the new action button shared the existing measurement form selector. No migration, Edge function or web source was deployed. The correction restores the prior whole-line issue transition and renders component actions outside the measurement form; fresh database and browser results are required.

The correctly isolated correction `7bc0236b8b5e84a80c63fd0355fd639cc2e63e2d` passed verification `34578050634`, browser `34578050609`, recovery `34578050619` and capacity `34578050664`. Database run `34578050615` passed all prior suites and the first 12 basket groups, then failed at simultaneous customer decisions. A diagnostic rerun `34578455115` retained raw attempts and proved this was not timeout pressure: PostgreSQL rejected a component decision because one `CASE` branch returned `json` while the other returned `jsonb`. The correction casts the frozen replacement line explicitly to `jsonb` and restores the full 16 simultaneous retries. A fresh complete database result remains required.

Database run `34578797818` then passed the 16-way component decision, exact reservation reallocation, fresh-measurement requirement and rejection path. It stopped only when the expiry fixture tried to rewrite protected substitution terms directly and correctly hit `immutable_substitution_history`. The disposable-superuser fixture now changes only `expires_at` while the history trigger is disabled and re-enabled inside one rollback-safe transaction; application functions, trigger protection and production privileges are unchanged. Fresh full database CI is required.

The expiry fixture in `3da48de7` reached successful expiry without changing its snapshot or stock, then failed when the test parsed an unquoted SQL state as JSON (`34579075248`). The corrected fixture uses `to_jsonb(state)` and follows the existing insertion-based time-travel pattern: it rejects the real proposal, inserts a separate past-expiry fixture, and keeps the immutable history trigger enabled throughout. It also verifies that rewriting an existing expiry is rejected.

A source-to-repository comparison found that prior connector uploads truncated the long evidence document and, in `fa14608c`, concatenated separate file encodings. The malformed review candidate was never deployed; the complete historical document is restored from the local source. Uploads now compare every returned Git blob hash with its local hash before branch advancement. A review of the actual picker binding found `dataset.component` instead of `dataset.componentSub`; the corrected click path is covered by a new full browser journey through selection, customer rejection, renewed proposal, explicit approval, fresh measurement and exact consumption. A competing-order PostgreSQL check also verifies that the losing approval retains the complete original order and reservation when only one replacement remains. Fresh gates are required for these corrections.

## Exact component replacement verified and backend applied — 11 September 2026

Corrected source **581c4f68c215b86ab8f15d50ffa43dbe724987a3** passed [verification **34579731602**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34579731602) (**293 Node tests**), the complete [PostgreSQL gate **34579731613**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34579731613) and [browser **34579731653**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34579731653) (**68 checks**). The basket database suite passed **18 groups**, retaining sixteen simultaneous decisions and adding last-replacement contention between two orders. The browser exercised actual picker selection, phone rejection, renewed proposal, explicit phone approval, fresh measurement and consumption. Expiry uses an inserted past-expiry fixture; the immutable history trigger remains enabled. The full staged local Git tree matched the remote candidate tree before branch advancement.

The SQL and Edge source bytes are identical to **fb6d14267022a8f9b7e12d94c8826899914a31f0**, which passed [populated recovery **34578797840**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34578797840) and [isolated capacity **34578797933**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34578797933). The mobile tree is identical to **f892c766f5ac5a8d5192233ce83288613e3fa814**, which passed [Expo **34577478080**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34577478080), [Android debug **34577477570**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34577477570) and [iOS simulator **34577477596**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34577477596). These source comparisons were checked with Git; they do not claim signed packages, physical-device acceptance or production capacity/recovery.

After those gates, Supabase applied **20260911083950_jana_customer_approved_basket_component_substitution** (**88 migrations**) and deployed ACTIVE **jana-api v32** from the tested four-file source. The SQL SHA-256 is `27dd320560c710502ec2ce963ec155be6c05f1bdb322a8b470d3dba1a1e5417e`; its repository filename is aligned to the actual application version without changing SQL bytes. `verify_jwt:false` remains intentional; jana-critical v6 and jana-ops-extra v22 are unchanged. Touched RPCs deny anon/authenticated execution, and private reallocation helpers deny service-role execution too.

All **19** pre/post business-table row counts and deterministic row hashes match exactly, including orders, stock, lots, slots, substitution/picking history, audit/events, returns, supplier credits, cost and cash ledgers. `jana_deep_health()` reports `ok:true` with every business invariant at zero violations. Advisor category and finding counts are unchanged (five existing security categories and two informational performance categories). No production fixture, customer decision or commercial admission change was made. At this backend checkpoint the published web remains **85a4511**; promotion and exact-commit public verification will be recorded separately.

## Exact component replacement published — 11 September 2026

The aligned release **bd9d224754e7755c0e07da31dbc566fde0b49ebe** passed all applicable review gates before fast-forward promotion: [verification **34580555916**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580555916), [database **34580555936**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580555936), [browser **34580555935**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580555935), [populated recovery **34580555937**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580555937) and [isolated capacity **34580555928**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580555928). Results are **293 Node tests**, **68 browser checks** and the complete database suite including **18 basket groups**. Recovery compared all **53 public tables** (24 nonempty). Capacity served 320 reads at concurrency 32 with p95 **155.6 ms**, then admitted 100 and shed 25 held requests before recovery. These are disposable-fixture results, not production capacity or real-backup RPO/RTO acceptance.

Both branches were checked immediately before promotion; `jana-live` advanced without force from `85a4511` to the exact tested release. No automatic or pending deployment appeared in two observations over a minute, consistent with this service's recorded public-clone behavior. One API deploy was requested without clearing cache or changing service settings. Render **dep-dahs0e9594qs738bqi5g** resolved the expected commit and became LIVE at **2026-09-11T08:48:49.766365Z**. [Public smoke **34580859338**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580859338), job **103203861522**, passed **26 checks** at **08:49:00 UTC**, including exact commit consistency, health, all Edge dependencies, catalog, assets and authentication boundaries.

The same release also passed production-branch [database **34580859341**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580859341), [browser **34580859280**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580859280), [recovery **34580859348**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580859348), [capacity **34580859290**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580859290), [Expo **34580859273**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580859273), [Android debug **34580859366**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580859366) and [iOS simulator **34580859332**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34580859332). All eight release workflows succeeded on the exact published commit. Android artifact **10191744978**, digest `sha256:792923373ff39e2edb86d72f813842d3ab125db8a1bef1ba793c455b9ab6d93a`, and iOS simulator artifact **10191683676**, digest `sha256:53632c5b9ba631bc212985493cf9d7cedfb5535cc46f690fabb77ef66c4c25e8`, expire **18 September 2026**. These are debug/simulator packages; native store signing and actual device acceptance remain outstanding.

Retrieving ACTIVE `jana-api` v32 after deployment confirmed that all four deployed files match the tested source byte for byte, retaining custom-session `verify_jwt:false`. The 19 business-table row counts and hashes still match after web publication (compared by table name, independently of response ordering); deep health remains true with zero violations. No error-level Render logs were found from deployment start through the first completed smoke. No production order, proposal, stock, cost or cash fixture was created or changed. Commercial intake remains closed. Remaining engineering and owner inputs are kept in AUDIT-MATRIX.md; no store release or commercial-launch completion is claimed.

## Operational health observations candidate — 11 September 2026

Review began at **ba4f0ef75646b5acd837a62a7f7cf3050cc073c8**, with **bd9d224** still LIVE on **dep-dahs0e9594qs738bqi5g**, 88 migrations and jana-api v32. All prior release gates and the documentation-only verification passed. The next evidenced operating gap was that the health page showed raw balance-invariant JSON without job liveness or expired work; stopping expiry could strand reservations without changing those invariant results.

The candidate adds read-only admin observations for scheduled expiry/reminder jobs, heartbeat/configuration failures and bounded overdue quote/substitution/reminder queues. The dashboard links to actionable Arabic phone-readable details. Refresh clears prior success, and failed or superseded responses cannot claim recovery. Database invariants and public readiness retain their existing meanings, auth remains custom-session/service-only, and no Edge source change is needed. Local and CI results, migration application and publication will be recorded after the candidate gates; production is unchanged at this source checkpoint. Independent monitoring and delivery to an approved alert destination remain outstanding.

Local `npm run check`, Python compilation and **300 Node tests** pass, including seven new observation UI behaviors plus the existing finance/operations tests. New disposable database and browser scenarios require fresh CI; their success is not inferred from local syntax checks.

Candidate **07a8d97ac06836a2a65f03561d3fd465599ec347** passed verification **34585636852**, populated recovery **34585636858**, capacity **34585636851**, and browser **34585636854** (**71 checks**, including phone alert/failure/recovery). Database **34585636891** correctly rejected the healthy baseline: the earlier saved-reminder test left its temporary two-second cron cadence behind. That disposable test now restores its complete original job configuration in `finally` and verifies equality; the production monitoring rule remains unchanged. The next candidate also makes sub-minute ages display seconds accurately. Production is still unchanged pending a complete passing database gate.

## Operational health verified and backend applied — 11 September 2026

Corrected candidate **9d3401bc760e96018c3c5d942413a95fcbb4fcec** passed [verification **34586314517**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34586314517) (**300 Node tests**), the complete [database gate **34586314540**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34586314540), and [browser **34586314543**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34586314543) (**71 checks**). The new database suite passed **13 groups**, including actual expiry-worker recovery, globally disabled scheduling, a 1,001-record backlog, all non-admin roles denied and sixteen concurrent read-only observations. The saved-reminder test now restores the prior job configuration exactly. The SQL, Edge, gateway and recovery/capacity inputs are byte-identical to the successful **07a8d97** recovery/capacity runs above; mobile source is unchanged from published **bd9d224**.

Supabase applied **20260911095523_jana_operational_health** (**89 migrations**). Its repository filename is aligned to the actual applied version; SQL SHA-256 remains `071a0d0505aa392522b47af83ad5ab65bf52db28ce00c73e525d36393c3aa36d`. Both scheduler observations report `ok`, all three overdue queues contain zero records, and the existing business health is true with zero violations. The private helper and admin wrapper grants passed; the observer executed in a read-only transaction. No Edge source or deployment is needed: jana-api v32 / jana-critical v6 / jana-ops-extra v22 retain custom-session authentication.

All **23** commercial/notification table counts and deterministic row hashes match before and after application, including quotes, orders, substitutions, stock, slots, costs, cash, returns, audit, in-app notifications, recurring plans and the disabled-channel outbox. Security/performance advisor categories and counts are unchanged. No fixture, synthetic heartbeat, manual production worker execution or outgoing message was used. Web publication is still pending at this backend checkpoint; `jana-live` and Render remain **bd9d224**. Independent alert delivery, dedicated staging/load, measured production-backup recovery and physical-device/store acceptance remain open.

## Operational health published — 11 September 2026

Aligned release **0dc5784e0fac03b668203668a1ef231aa40fd3e4** passed all five applicable review workflows before promotion: [verification **34586831983**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34586831983), [database **34586831948**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34586831948), [browser **34586831962**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34586831962), [recovery **34586831942**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34586831942) and [capacity **34586832007**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34586832007). These include **300 Node tests**, **71 browser checks**, the complete PostgreSQL suite and **13 operational-observation groups**. The full local Git tree matched the remote release tree. Native and Edge source did not change; existing native compile evidence remains the exact-source **bd9d224** evidence above, not a newly run build or signed store release.

Both remote heads were checked immediately before fast-forwarding `jana-live` from **bd9d224**. Two observations over 86 seconds found no automatic/pending deployment, consistent with the existing documented public-URL clone. One API deploy was requested with no cache clearing or service-setting change. Render **dep-daht3jbm8hqs73d9hj70** resolved the exact release and became LIVE at **2026-09-11T10:03:23.215684Z**. [Public smoke **34587134163**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34587134163), job **103223874531**, passed **26 checks** at **10:03:32 UTC**, including the exact deployed commit, dependencies, authentication, catalog and assets. The production-branch [database **34587134147**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34587134147), [browser **34587134157**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34587134157), [recovery **34587134130**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34587134130) and [capacity **34587134179**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34587134179) also passed on this release.

After publication, all **23** monitored table counts/hashes still match the pre-application baseline. Business invariants report zero violations; both scheduled jobs report recent success and all three overdue queues remain empty. Supabase remains at **89 migrations** with the existing ACTIVE Edge versions and custom-session authentication. No error-level Render logs appeared between **10:02:50 and 10:04:13 UTC**. The existing `no-store` API/client policy keeps observations out of caches. The admin alert/failed-refresh/recovery UI was exercised through the real isolated gateway/Edge/database chain; production checks stayed read-only and created no test account, quote, order or notification.

This closes the in-app observation gap only. There is no unattended monitoring or external alert delivery while the dashboard is closed. Approved monitoring destination/provider and escalation ownership, dedicated JANA staging/load, real-backup RPO/RTO, warehouse routing/segregation, approved commercial catalog/stock/geography, actual device acceptance and signed store distribution remain explicit work items in AUDIT-MATRIX.md. Commercial admission remains closed.

## Owner handoff and bookmarked setup candidate — 11 September 2026

Work began from review **9e4f0ba70f1077c009bee4f88e89c3fc0324c220**, after confirming its successful verification and the published **0dc5784** source/deploy/gates above. Read-only production checks found admission closed, no published merchant profile, eight active preview products and healthy operational observations. This is evidence that merchant/stock acceptance is still required, not permission to invent those inputs.

The candidate adds a public `/start.html` team-link hub and admin-only `/admin.html#launch` setup center using the existing read-only storefront/deep-health endpoints. It distinguishes actual admission state, recorded observations, unavailable reads and manual acceptance. Role-permitted section bookmarks survive sign-in/reload/history navigation. Unknown or unauthorized sections cannot request admin launch data. Merchant drafts warn before navigation, refresh or logout, freeze fields during save, and reject obsolete page/session read completions. No schema, Edge, custom-session, mobile or commercial-admission rule changes are included.

Local syntax/bundle checks and **309 Node tests** pass, including eight launch/navigation/stale-read behaviors and a gateway check that the handoff links remain available without backend reads. The expanded disposable browser journey will exercise phone layout, owner login/bookmarks/history, unsaved-draft cancellation, read failure/recovery, fixed in-flight save fields and finance role isolation. Browser/capacity CI and exact-commit public smoke are still pending at this candidate checkpoint. All fixture traffic remains inside the existing guarded local gateway/Edge/PostgreSQL chain. The Arabic [owner handoff guide](OWNER-HANDOFF-ar.md) lists exact role links, setup order and inputs still requiring the owner; no readiness percentage or store download link is fabricated.

Candidate **646b581e3117b0fb5da4a8a5b3938e2dac490f01** passed [verification **34590137125**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590137125) and [capacity **34590137133**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590137133). [Browser **34590137287**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590137287) passed the new owner phone/bookmark/draft/failure scenarios and then exposed a real navigation regression: clicking the already selected orders section no longer refreshed state after another staff member finished picking. Courier assignment therefore used stale order state. Active-section clicks now refresh clean pages, while same-section dirty merchant drafts and duplicate hash events retain their current contents. A dedicated Node regression covers both behaviors. This failed browser gate remains recorded; production is unchanged until the corrected candidate passes.

## Owner handoff published — 11 September 2026

Corrected source **f0a92d8b11b5e93b64b77c281bb656f6bb78cd38** passed [verification **34590557063**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590557063) (**310 Node tests**) and the complete [browser journey **34590557055**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590557055) (**76 checks**) before promotion. The browser test confirms phone-width links/layout, owner bookmark preservation across actual login/reload/back/forward, no commerce mutations from setup reads, cancelled draft navigation, unknown/recovered admission reads, fixed in-flight draft fields, finance role isolation and the complete customer/warehouse/picker/courier/cash/support journey. Browser evidence artifact **10195498002**, digest `sha256:826c8fea9ea11d6d9e46b495a5103e8f7e116c9758ea61dd21f5083f3c6f22c2`, expires **25 September 2026**. It contains disposable fixture screenshots, not real customer data. Direct screenshot-download attempts in this workspace returned HTTP 403; screenshot capture/phone-width assertions are CI evidence, while the published public hub was separately inspected in a live browser.

Capacity-sensitive source is byte-identical to the successful **646b581** [capacity **34590137133**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590137133): 320 reads at concurrency 32, p95 **147.2 ms**, 100 admitted and 25 rejected overload requests followed by recovery. Supabase and mobile source trees are unchanged from **0dc5784**. Their earlier database/populated-recovery/native evidence above therefore remains applicable to unchanged source; no new full database, recovery, Expo or native run is claimed for this web-only change.

Both branch heads and Render service/suspension/deploy state were checked before fast-forwarding `jana-live` from **0dc5784** at **10:45:37 UTC**. Two subsequent deployment observations through **10:46:50 UTC** found no automatic or pending deployment. One API deploy, with no cache clearing or service changes, resolved the tested commit. Render **dep-dahto7ad0e5s738teiag** became LIVE at **2026-09-11T10:47:22.11679Z**. [Public smoke **34590772038**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590772038), job **103235347814**, passed **27 checks** at **10:47:34 UTC**, including the new `/start.html` public asset and exact deployed commit. Production-branch [browser **34590772021**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590772021) and [capacity **34590771974**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34590771974) also succeeded on this source.

Before/after read-only snapshots match row counts and deterministic hashes for **48 public commercial/configuration tables**, compared by table name and fields rather than JSON property order. This includes storefront state/profiles, catalog, stock, orders, addresses, financial/return ledgers, notification configuration and audit. Session/rate-window/worker runtime tables and the platform spatial dictionary are outside this commercial comparison. There are no production fixtures or business writes in this release. No error-level Render logs appeared from **10:46:50 to 10:49:00 UTC**. Supabase remains at **89 migrations** with unchanged Edge/custom-session behavior.

The published public hub renders correctly and its owner link reaches the normal employee sign-in page while preserving `#launch`. Private owner behavior was verified through isolated authenticated browser fixtures, not by using a production user's credentials. Commercial admission remains closed, with no published merchant profile and eight active preview products. The [Arabic owner guide](OWNER-HANDOFF-ar.md) is ready to hand over for setup: actual merchant/catalog/stock/coverage approval, provider and alert destinations, independent staging/real-backup recovery, physical devices and store signing remain outstanding. No claim of immediate sales readiness or signed mobile distribution is made.

## Atomic catalog draft import candidate — 11 September 2026

Work starts from review documentation `4d56e4c` and published web `f0a92d8`. The candidate adds an admin-only JSON template, browser validation against current active stock identities and one idempotent PostgreSQL transaction for up to 50 new product families / 500 sellable sizes. Every result is an inactive immutable draft. Nothing is activated, repriced or added to inventory by import; the owner must review and activate each version separately. Duplicate names inside the file, unknown/inactive stock, duplicate size/component identities, unsafe image URLs and invalid limits are rejected. A failure in any later product rolls back the entire file, and a retry with the same key returns the original drafts rather than duplicating them.

Local syntax/bundle checks and **313 Node tests** pass. Dedicated UI tests cover the empty example template, normalization/count summary and unsafe/unknown/duplicate inputs. The disposable PostgreSQL gate adds atomic rollback, role/grant, inactive draft, changed-retry and duplicate-name coverage. Docker is unavailable in this workspace, so database/browser CI, migration/Edge application and publication are not claimed at this checkpoint. No production data, schema, Edge function or Render release has changed.

Initial browser run [34597378587](https://github.com/NAIFMUSFER/Heavy/actions/runs/34597378587) failed before authentication because the new imported browser module was omitted from the gateway's explicit public-file allowlist; the admin module therefore never initialized. The gateway now serves only that named module, routes only the exact `/api/ops/products/import` path to the operations Edge service, and has direct regression coverage for both boundaries. This failure is retained; corrected CI is required before promotion.

## Atomic catalog draft import published — 11 September 2026

Corrected source **0faf8656eecd47924c409414c7717a6d178dd4f5** passed [verification **34597754268**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34597754268) (**313 Node tests**), corrected [browser **34597754275**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34597754275) (**76 checks**) and [capacity **34597754283**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34597754283). The identical migration/Edge source had already passed the complete [PostgreSQL gate **34597378631**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34597378631) and populated [recovery **34597378544**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34597378544). Tests cover browser validation/template output, exact gateway routing, all-or-nothing multi-product creation, inactive drafts, request-key replay/conflict, later-product rollback, duplicates, roles and direct-client grant denial.

After checking both branch heads and finding no pending Render deployment, `jana-live` was fast-forwarded from **f0a92d8** to the tested source without force. Production-branch [database **34598305653**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34598305653), [browser **34598305641**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34598305641), [recovery **34598305743**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34598305743) and [capacity **34598305666**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34598305666) all passed on the same commit. Render's configured automatic trigger did not create a deployment during the full smoke wait, so the first live-smoke attempt failed only on the still-published prior commit. With no deployment pending, one API deployment was requested without clearing cache or changing service settings. Render **dep-dahv6pijnfac73a9bek0** became LIVE at **2026-09-11T12:26:43.673836Z**; retrying only the failed job made [exact-commit public smoke **34598305741**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34598305741) pass **27 checks**. No error-level Render logs appeared from **12:25:30 to 12:28:00 UTC**.

Supabase applied **20260911120000_jana_atomic_catalog_draft_import** (**90 total migrations**) and ACTIVE `jana-ops-extra` is **v23**; `jana-api` v32 and `jana-critical` v6 are unchanged. All retain the intentional `verify_jwt:false` custom-session boundary. Direct anon/authenticated execution of the import RPC is denied and service execution is allowed. All **47** monitored commercial/configuration table counts and deterministic hashes matched before and after migration/Edge deployment. A fresh post-web health read reports zero negative stock, over-reservation, overbooked slots, duplicate quote orders or cash-ledger/invariant violations. No production product, offering, inventory, order, account or financial fixture was created.

The feature is published at `/admin.html#catalog`, but import remains a preparation step: every imported version is inactive until individually reviewed and activated, and the owner must replace preview catalog data with approved products, prices, images and actual inspected stock. Commercial admission remains closed until the merchant, tax/policy, geography/capacity, provider, operating/device and signed-store inputs in OWNER-HANDOFF-ar.md are completed.

## Same-browser cart synchronization candidate — 11 September 2026

Work starts from review documentation **25c8b275** and published web **0faf8656**. The proven gap was two storefront tabs sharing one local cart without a cross-tab serialization boundary: each tab serialized only its own writes, so simultaneous quantity edits could overwrite one another. The shared web/native persistence controller now accepts an optional exclusive lock and external-change subscription. The web client uses the browser's same-origin Web Locks and storage events, rereads the committed cart inside the lock, applies the user's edit to that latest value and repaints both the header and an open cart. Invalid external storage cannot replace the last valid visible selection. Mobile behavior and the stored array format remain unchanged; cross-device/account-cart merging is still explicit.

Local bundle/syntax checks and **315 Node tests** pass. New controller tests run two stores against one lock/storage channel, prove two simultaneous additions compose and reach both snapshots, verify cleanup and retain a valid cart after damaged external data. The disposable Playwright journey now opens two pages in one browser context, submits concurrent real storefront clicks and requires both pages plus localStorage to show the composed quantity. No database migration, Edge function, provider, production business record or commercial-admission setting changes in this candidate.

## Same-browser cart synchronization published — 11 September 2026

Corrected source **e101198c40cb26e9f441c316eb948ee328c51d74** passed review [verification **34601484196**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34601484196) (**315 Node tests**), [browser **34601484199**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34601484199) (**77 checks**), [Expo **34601484203**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34601484203), [Android debug **34601484201**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34601484201) and [iOS simulator **34601484187**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34601484187). The browser gate uses two pages in one actual browser context and verifies both visible counts and the committed localStorage quantity after concurrent storefront clicks. Invalid external data retains the last valid cart and exposes the explicit recovery path. Native source remains compatible but this is not a signed store build or physical-device acceptance.

Immediately before promotion, `jana-integrity` was two commits ahead of `jana-live`, zero behind, with merge base exactly the published **0faf8656** release. `jana-live` advanced without force to the tested source. Render showed no queued or automatic deployment after more than five minutes, so one API deployment was requested without clearing cache or changing service settings. Render **dep-dahvs3nqj5pc73aopsig** resolved the expected commit and became LIVE at **2026-09-11T13:12:06.374122Z**.

The first production smoke attempt ended its readiness wait seconds before that delayed deployment started; it remains recorded rather than relabelled as a pass. Only the failed job was rerun. [Production verification and smoke **34602325362**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34602325362), attempt 2, passed **27 checks** at 13:13 UTC against the exact public commit. Production [browser **34602325376**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34602325376), [Expo **34602325429**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34602325429), [Android debug **34602325507**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34602325507) and [iOS simulator **34602325383**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34602325383) all passed on the same commit. Render returned no error-level log entries from deployment start through post-smoke verification.

This release changes only static/shared cart behavior and service-worker cache version. Supabase remains at **90 migrations** and ACTIVE `jana-api` v32 / `jana-critical` v6 / `jana-ops-extra` v23 with custom-session authentication and intentional `verify_jwt:false` unchanged. No SQL, Edge function, provider, production order, inventory, money, account, password or commercial-admission setting was written. Cross-device/account-cart merge remains an explicit revisioned save/restore action. Multi-warehouse inventory ownership and routing remain a separate coherent backend project and were not represented by a cosmetic warehouse field.

## Privacy-minimized launch report published — 11 September 2026

Starting from review documentation **a47ab8ec** and published **e101198c**, the owner launch center now prepares a downloadable `jana-launch-readiness.json` observation from the same read-only storefront and operational-health responses already displayed. The bounded schema records only admission state, storefront/policy revisions, seven check states and action links, plus known operational check time/result/count. It intentionally excludes merchant draft fields, policy text and contact details; unknown responses remain `unknown`/`null`, manual acceptance remains explicitly required, and no readiness percentage is calculated. Downloading the data URI performs no server write.

Source **219469f32c240c618bdbbcc751f36cdd5c9af609** passed review [verification **34604562804**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34604562804) (**316 Node tests**) and the complete [browser journey **34604562794**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34604562794) (**78 checks**). The browser journey parses the actual data-URI download, requires all seven check records, proves the privacy boundary and compares commerce state before and after. The production-branch [browser journey **34604839566**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34604839566) also passed **78 checks** on the identical source.

Immediately before promotion, `jana-integrity` was two commits ahead of `jana-live` and zero behind. `jana-live` advanced without force. Render's configured automatic trigger did not create a deployment during the full smoke readiness wait, so the first smoke attempt failed only because the previous release remained public. With no deployment pending, one API deployment was requested without clearing cache or changing service configuration. Render **dep-dai09eajnfac73adgm00** became LIVE at **2026-09-11T13:40:55.605973Z**. Retrying only the failed job made [verification and exact-commit public smoke **34604839540**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34604839540), attempt 2, pass **27 checks** against the published commit. Render returned no error-level entries from **13:40:00 to 13:42:30 UTC**.

No database migration, Edge function, provider, mobile source, production business record or commercial-admission setting changed in this release. Supabase remains at **90 migrations** with ACTIVE `jana-api` v32, `jana-critical` v6 and `jana-ops-extra` v23; custom-session authentication and intentional `verify_jwt:false` are unchanged. Supabase migration work was not started because the approved CLI migration generator is unavailable in this workspace; no migration identifier or warehouse assignment was invented. Multi-warehouse ownership/routing remains open as a coherent backend change rather than a cosmetic field.

## Excel/CSV catalog intake published — 11 September 2026

Work starts from review documentation **55a35094** and published **219469f3**. The catalog intake now offers a UTF-8 Arabic CSV template for Excel alongside the existing JSON format. CSV prices are entered in riyals with exact two-decimal Arabic/Persian/Latin digit normalization; quantities remain integer base units. Rows group by explicit product and sellable-size keys, support multiple components without repeating display terms, retain quoted commas/newlines and reject changed repeated terms, duplicate components, unknown stock, unsafe images, altered headers, excess columns and malformed quoting. Template guide rows and active stock identities are comments and cannot create products by themselves.

Both formats produce the existing validated `schema_version:1` payload and use the same idempotent all-or-nothing database transaction. Previewing a file enables no product, inventory or sales state. Source **2684366a2a46c00be19e144165207128ee99bff0** passed review [verification **34608026645**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34608026645) (**319 Node tests**) and [browser **34608026740**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34608026740) (**79 checks**). The browser selects a real CSV file on the owner path, verifies exact Arabic riyal conversion and confirms no database write before the separate checkbox/submission action.

Immediately before promotion, review was two commits ahead of production and zero behind; `jana-live` advanced without force. Render created no automatic or pending deployment during the complete smoke readiness wait. With the prior release still public, one API deployment was requested without clearing cache or changing service configuration. Render **dep-dai0q6ad0e5s7399gndg** became LIVE at **2026-09-11T14:16:20.562280Z**. Retrying only the failed smoke job made [exact-commit production verification and smoke **34608325197**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34608325197), attempt 2, pass **27 checks**. Production [browser **34608325219**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34608325219) passed **79 checks** on the same source. Render returned no error-level entries from **14:15:45 to 14:18:00 UTC**.

This release changes no SQL, Edge function, provider, native source, production product, inventory, order, money, account or commercial-admission setting. Supabase remains at **90 migrations** with ACTIVE `jana-api` v32, `jana-critical` v6 and `jana-ops-extra` v23; custom-session authentication and intentional `verify_jwt:false` are unchanged. The published CSV path is preparation only: actual owner data, photographs, reviewed activation and inspected stock remain required.

## Owner catalog and inventory review exports published — 11 September 2026

Work starts from published source **2684366a** and review documentation **c7d15aea**. The catalog page now downloads a BOM-prefixed Arabic `jana-catalog-review.csv` containing current families, immutable versions, sellable sizes, exact riyal prices, state and stock components. The inventory page downloads `jana-inventory-review.csv` with a UTC observation time, stock totals, available/sellable/reorder status and lot receipt/inspection/expiry facts. The second file intentionally excludes supplier phone/contact details and inventory costs. Both are computed from the already-authorized read response, perform no API mutation and are deliberately separate from the catalog import format and from count-session approval.

All untrusted text cells that could be interpreted as spreadsheet formulas after leading whitespace are escaped before CSV quoting. Unknown numeric values stay blank rather than becoming zero. Draft versions without offerings and legacy unlinked offerings/lots remain visible instead of being silently omitted. Source **9e487cd512a4a2eb04b6606b71a5e85674036c49** passed review [verification **34613413870**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34613413870) (**323 Node tests**), [browser **34613413852**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34613413852) (**81 checks**) and [capacity **34613413711**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34613413711). The browser downloaded both actual files from isolated authenticated owner/inventory interfaces, checked the BOM and expected columns, and proved no catalog/commerce or stock mutation from the downloads.

Immediately before promotion, `jana-integrity` and `jana-live` heads were re-read; production was still **2684366a** and the review candidate was unchanged. `jana-live` advanced without force. Render's configured automatic trigger did not create a deployment after two observations, and no deployment was pending, so one API deployment was requested without clearing cache or changing service settings. Render **dep-dai1glm743jc73djbcs0** became LIVE at **2026-09-11T15:04:18.490745Z**. Production [verification and exact-commit smoke **34613774018**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34613774018) passed **323 tests** and **27 public checks** against the published commit. Production [browser **34613774050**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34613774050) passed **81 checks**, and [capacity **34613773839**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34613773839) also succeeded. Render returned no error-level entries from **15:03:30 to 15:05:30 UTC**.

This is a static web release. No SQL, Edge function, provider, native source, production product, inventory, order, money, account or commercial-admission setting changed. Supabase remains at **90 migrations** with ACTIVE `jana-api` v32, `jana-critical` v6 and `jana-ops-extra` v23; custom-session authentication and intentional `verify_jwt:false` are unchanged. Actual merchant, catalog, inspected stock, geography/capacity and operating approvals remain required, as do selected providers, real backup recovery, physical-device acceptance and signed store releases.

## Atomic inactive stock-master import published — 11 September 2026

Source **30b3c40cdba9a685aeb801f650babc6cade273f4** adds Arabic Excel/CSV and JSON templates to the inventory page for at most 200 stock definitions. The browser validates names, optional English names, categories, `gram`/`piece` units and nonnegative integer reorder thresholds, including Arabic/Persian digits. It rejects identifiers, active state, quantities, reservations, duplicates and unknown fields. The single database transaction revalidates the payload under an advisory lock, inserts inactive stock items and zero balances only, records one audit event, and provides durable idempotent replay. It never creates lots, movements, costs, supplier terms or sellable stock.

The exact source passed review [verification **34620135504**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620135504) (**328 Node tests**), [PostgreSQL **34620135499**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620135499), [browser **34620135355**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620135355) (**82 checks**), [populated recovery **34620135239**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620135239) and [isolated capacity **34620135423**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620135423). Recovery matched all 53 public-table fingerprints. Capacity served 320 fixture reads at concurrency 32 with p95 125 ms, then admitted 100, shed 25 and recovered. These are isolated-fixture results, not production RPO/RTO or capacity acceptance.

After the gates, Supabase applied **20260911161256_jana_atomic_stock_master_import** (**91 migrations**) and deployed ACTIVE **jana-ops-extra v24** from the exact two tested files. `jana-api` v32 and `jana-critical` v6 are unchanged. All retain intentional `verify_jwt:false` behind custom sessions. The new SECURITY DEFINER routine has a fixed `public, extensions, pg_temp` search path, denies anon/authenticated execution and permits service-role execution. The advisor categories remain the existing five security and two informational performance categories.

`jana-live` advanced without force. No automatic Render deployment appeared during the observation window, so one API deployment was requested without clearing cache. Render **dep-dai2j50jo6nc73d5sjkg** became LIVE at **2026-09-11T16:17:50.253587Z**. Production [verification and exact-commit smoke **34620987581**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620987581) passed **328 tests** and **27 public checks**. Production [database **34620987652**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620987652), [browser **34620987620**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620987620), [recovery **34620987566**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620987566) and [capacity **34620987580**](https://github.com/NAIFMUSFER/Heavy/actions/runs/34620987580) also passed on the same commit. Render returned no error-level entries from 16:17:00 through 16:19:30 UTC.

All **47** monitored production commercial/configuration table counts and deterministic hashes matched before migration, after Edge deployment and after web publication. Deep health remained `ok:true` with every business invariant at zero. Stock remained eight items/eight balances/eight lots/zero movements; no definition, quantity, cost, lot, supplier, order, account, money or admission fixture was created. Owner import is now available at `/admin.html#inventory`; actual approved definitions, activation, inspected receipt/count evidence and multi-warehouse segregation remain separate prerequisites.
## Single-hub warehouse routing candidate — 11 September 2026

Starting from production `30b3c40`, this review candidate adds private canonical warehouse records and delivery-zone ownership without seeding or guessing any production location. Warehouse activation requires a name, city, operational address and coordinates; audited revision checks and a partial unique index enforce one active hub. Zone writes assign the active warehouse in the same idempotent transaction, slot writes require a live route, and launch readiness excludes unlinked slots and blocks commercial admission. Deactivation while intake is open is rejected. RLS is enabled and direct anonymous/authenticated table and helper execution remains revoked; custom application sessions remain unchanged.

The operations UI shows the launch hub, its actual recorded location state and each zone owner, while explicitly stating that per-hub stock segregation is not implemented. The launch report adds warehouse routing as a distinct check. Local `npm test` passed 328 tests before the dedicated UI assertion was added; focused gateway/UI checks then passed 39 tests, and the complete source now contains 329 Node tests. PostgreSQL replay/concurrency, full browser journey, native builds, Supabase application, Edge deployment and Render publication are not claimed until their gates complete. No production data has been changed at this candidate checkpoint.
