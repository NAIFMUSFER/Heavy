export function mergeSavedCart(cart,items,catalog){
 const next=cart.map(x=>({...x}));for(const item of items){const p=catalog.find(p=>p.family_id===item.offering_family_id);if(!p)throw Error((item.name||'الصنف')+' غير متاح حاليًا');const index=next.findIndex(x=>x.family_id===p.family_id||x.offering_id===p.id);const quantity=(index<0?0:next[index].quantity)+item.quantity,limit=p.orderable===true&&p.fulfillment_model==='supplier_pickup'?p.max_order_quantity:p.available_units;if(!Number.isInteger(quantity)||quantity<1||quantity>20||!Number.isInteger(limit)||quantity>limit)throw Error('الكمية المطلوبة غير متاحة: '+p.name);const line={offering_id:p.id,family_id:p.family_id,quantity,name:p.name,price_halalas:p.price_halalas,emoji:p.emoji,size_label:p.size_label};if(index<0)next.push(line);else next[index]=line;}if(next.length>40)throw Error('الحد الأقصى للسلة 40 صنفًا');return next;
}
export {saudiDateValue as saudiReminderDate} from './input.mjs';
